# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Duskline is a client-side web app for landscape lighting contractors: design a lighting layout on top of a customer's photo (drag-and-place fixtures, aim beams, draw wire runs and LED strips) and generate a customer quote / PDF from the same data. There is no build system and no package manager — the UI is one file, `index.html` (renamed from `duskline-lighting-designer_21.html` for Phase 4 — static hosts serve `index.html` at the root URL with zero config; ~3300 lines: inline `<style>`, inline `<script>`, no external JS files except CDN-loaded SheetJS and `@supabase/supabase-js`). It has a real Supabase backend for auth, app data, and photo/logo storage now (see "Auth & backend" below) — `localStorage` is no longer used for anything except small UI prefs (theme).

This directory **is now a git repository** (initialized as part of the backend migration below), but has no `package.json`, no test suite, and no linter configured. Treat every change as a direct edit to the one HTML file.

The two markdown files here (`duskline-going-live-roadmap.md`, `duskline-subscription-launch-plan.md`) are advisory planning documents (not app docs), written before any backend existed, describing a phased plan to move this from a local-only file to a hosted multi-user product on Supabase with Stripe billing eventually. **Phases 1–4 of that plan are now implemented**: real accounts, projects/fixtures/company data in Postgres, photos/logo in real file storage, and the app is live at `https://duskline.ca` (see "Auth & backend (Supabase)" below, and the migration-progress checklist for deploy details). Billing (Phase 5) and ongoing hardening (Phase 6) are still not implemented.

## Backend migration progress

Tracking status against the phased plan in `duskline-going-live-roadmap.md`. Update this checklist whenever a phase's status changes — it's the fastest way for a future session to know where things stand without re-reading the whole roadmap doc or guessing from the code.

- [x] **Phase 0 — product shape decided**: multi-tenant SaaS (not just a solo or single-team tool). Every signup creates its own company/tenant; there's no invite-an-existing-company flow yet.
- [x] **Phase 1 — auth + bare database**: Supabase project created (`bolwcxusbfsizsjtulvr`, see `SUPABASE_URL` in the HTML file and `supabase/schema.sql` for the schema). Real sign-up/login/logout works end-to-end, manually tested (signup → company auto-created → account panel shows company+role → logout → login). Committed as `c0164d5`.
- [x] **Phase 2 — migrate persistence**: `projects`, `areas`, `custom_fixtures`, `fixture_overrides` tables added, plus branding/quote-appearance columns on `companies` (all in `supabase/schema.sql`). The app reads/writes these instead of the Phase-1 localStorage blob — see "Auth & backend (Supabase)" below for how loads/saves are scoped. Manually tested end-to-end: create project → add area → increment an accessory quantity → close editor → log out → log back in → project/area/accessory data all reload correctly from Postgres (confirmed both via the UI and by querying the REST API directly). Committed as `5f178b5`.
- [x] **Phase 3 — move photos to real file storage**: a public `photos` Storage bucket (see `supabase/schema.sql`) replaces inline base64 for both area photos and the company logo — `areas.photo`/`companies.logo` now hold a Storage public URL instead of a data: URL (existing base64 rows keep working untouched; there's no backfill, they migrate the next time that photo is changed). Bucket is public by deliberate choice (confirmed with the user): simpler than signed URLs, privacy comes from an unguessable per-company path prefix rather than a login check. Manually verified: upload/RLS/public-URL flow works end-to-end via direct Storage API calls (couldn't drive the actual file-picker UI through browser automation), RLS confirmed to reject a write outside the caller's own company path, and a real bug was caught and fixed in the process — see the `openCropScreen` note below. Committed as `4261484`.
- [x] **Phase 4 — deploy + domain**: live at `https://duskline.ca`, deployed via Vercel connected to the GitHub repo `isaaccoombs550/Duskline` (branch `main`) for auto-deploy on every push — no manual re-upload step needed for future changes. `duskline-lighting-designer_21.html` was renamed to `index.html` first, since static hosts serve that filename at the root URL with no config needed. Manually verified on the live production URL: sign-in works, session persists, company/profile data loads correctly, no CORS issues between Vercel and Supabase. Domain bought through Porkbun (`.ca`, so it required confirming Canadian presence at registration) and DNS points at Vercel — root domain is an `A` record to `216.198.79.1`, `www` is a `CNAME` to `cname.vercel-dns.com`, and Vercel 308-redirects the root to `www`. The underlying `https://duskline-phi.vercel.app` URL still works too; the custom domain is just an alias on top of it. The team briefly considered renaming the product away from "Duskline" (worried a mood-word name wouldn't be findable as landscape lighting software) but decided to keep it and lean on a clear tagline/positioning instead — no code or naming changes resulted from that discussion.
- [ ] **Phase 5 — billing (Stripe)**: only worth doing once there's real demand — see the subscription-launch-plan doc's "validate first" recommendation. Not started.
- [ ] **Phase 6 — ongoing hardening**: backups, error monitoring, periodic RLS review. Not started.
- [x] **Distributor accounts**: schema + app code written and SQL run against the live Supabase project (see "Distributor accounts" in the Auth & backend section below for the full design — `companies.account_type`, `distributor_links`, `distributor_invites`, `contractor_catalog()`, the reused-`custom_fixtures`-as-catalog decision, the access-code overload, the new Contractors screen). Distributor signup (`DUSKLINEDISTRIBUTORV1`) works and confirmed via testing. Contractor signup via a generated invite code failed every single time until a real bug was found and fixed — see `handle_new_user()`'s `v_invite_found`/`FOUND` comment for the "testing a partially-null record with `IS NOT NULL`" gotcha (every unredeemed invite has some null fields, so `v_invite is not null` was always false even when a row was genuinely found). Confirmed fixed and working live, contractor/distributor name display and the distributor-chosen label also confirmed working after their own RLS fix. **Still not verified end-to-end**: a distributor changing a fixture's price/a contractor's multiplier actually reaching the contractor live (the core "not a one-time copy" promise) — worth a deliberate pass.
- [ ] **Admin dashboard, contractor removal, password reset**: schema + app code written, **SQL not yet run against the live project**. See the "Platform admin dashboard" and "Password reset" notes in the Auth & backend section below. Once the SQL is run, Isaac still needs to manually flag his own `profiles` row with `is_platform_admin = true` (no UI does this) before the Admin tab will appear for him, and the password-reset flow needs the Supabase dashboard's allowed-redirect-URLs list checked (not done from this environment).
- [ ] **Security Advisor fix — `contractor_catalog_view`/`distributor_contractor_view`/`contractor_distributor_view` converted to `security definer` functions**: schema + app code written (`index.html` now calls `sb.rpc('contractor_catalog')` etc. instead of `sb.from(view).select('*')`), **SQL not yet run**. See the Security Advisor note in the "Distributor accounts" section below. **Run this SQL before (or immediately after, but ideally before) deploying the corresponding `index.html` change** — the client code and the database need to agree on views-vs-functions, or fixture-catalog/contractor-list loading for distributor accounts will silently come back empty until both sides match.

**Signup is currently gated behind an access code** (`DUSKLINEBETAV12026`, hardcoded in the `handle_new_user` Postgres trigger, see `supabase/schema.sql`) — added to stop uncontrolled signups while the app is actively being changed, since some people already know the URL. Enforced server-side in the trigger (not just a client-side field check), so it can't be bypassed by editing the page's JS — a wrong/missing code makes the whole trigger transaction roll back, so the `auth.users` insert itself fails and the client sees an error, rather than an orphaned auth user with no company. To change the code or remove the gate once broader signups are wanted, re-run the `create or replace function public.handle_new_user()` block in `supabase/schema.sql` via the SQL Editor (update the code, or delete the `if` check entirely), and remove/update the `#authAccessCodeField` input in `index.html` to match.

Other open items worth resolving before real customers sign up (not blocking further dev work):
- **Email confirmation** is currently OFF for this Supabase project (test signups return an active session immediately, no email-verification step) — fine for development, but decide deliberately before production.
- No invite flow exists for adding a second person to an existing company (every signup makes a new tenant) — needed once "team seats" matters.
- **Async saves can lose the last few seconds of an edit on tab close.** Phase 1's `beforeunload`/`pagehide` saves were synchronous `localStorage` writes, so they always completed. Phase 2's saves are network calls, which aren't guaranteed to finish before the page actually unloads. The 5s autosave tick plus explicit flushes when navigating away (closing the area editor, switching projects, logging out) keep the window small, but closing the tab within ~5s of an edit and before navigating away can still lose that edit. Worth a `sendBeacon`-based fix if it turns out to matter in practice; not done.

## Running / testing changes

There's no dev server or build step. To check a change:
1. Open `index.html` directly in a browser (double-click, or serve the folder with any static file server).
2. Exercise the feature manually — there are no automated tests. Check both the "Customer" and "Internal" quote-mode tabs, light and dark theme, and (for anything touching layout/export) the print/export path via the "Print / Export project" button.
3. All app data now lives in Supabase, not `localStorage` — clearing site data / using a private window no longer gives a clean slate for a given account, since a login re-fetches everything from Postgres regardless of what's in the browser. To test "fresh account" flows, sign up a new test account instead.
4. The whole app requires the Supabase project to be reachable (real network calls to `https://bolwcxusbfsizsjtulvr.supabase.co` for both auth and data) — nothing works offline anymore, including opening a project you already created. Test accounts/projects created while testing should be cleaned up from the **Supabase dashboard → Authentication → Users** (deleting a user cascades to `profiles` → their `companies` row isn't auto-deleted by that cascade, since `companies` doesn't reference `auth.users` — delete stray `companies`/`projects` rows manually via **Table Editor** if you want a fully clean slate, or just leave them, since RLS means no other account can ever see them).

## Architecture (single IIFE, no framework)

Everything lives inside one `(function(){ "use strict"; ... })()` in the trailing `<script>` block (starts ~line 815). There's no component framework, no virtual DOM, no bundler — UI updates happen via direct DOM manipulation (`document.getElementById`, aliased as `$()`) and manual `render*()` functions that rebuild `innerHTML` for a section when state changes. Keep this style consistent rather than introducing new patterns for isolated features.

### State shape

A single in-memory `state` object (declared ~line 840) holds everything:
- `state.projects[]` → each project has `areas[]`, plus customer info and quote settings. Each area has `photo`, `placements[]` (fixtures dropped on the photo), `lightStrips[]`, `wireRuns[]`, and lighting/brightness sliders.
- `state.customFixtures[]` / `state.fixtureOverrides{}` — user-added or user-edited fixtures, layered on top of the built-in `BASE_FIXTURES` catalog (~line 820) via `allFixtures()`.
- `state.company{}` — company branding + PDF/quote appearance settings (logo, accent color, tax rate, PDF section order).
- `state.activeProfile` — the signed-in Supabase user's id. Now mostly vestigial (it predates Phase 2's real data layer) — truthy-checked in a couple of places as a loose "is someone logged in" signal, but `authUser` (see below) is the actual source of truth for that.

`state` itself still gets mutated in place and re-rendered via `renderX()` functions exactly as before Phase 2 — that part of the architecture didn't change. What changed is where mutations get *persisted*: instead of one `localStorage.setItem` of the whole object, saves now go to Supabase, scoped to just the company/project/area actually being edited (see "Auth & backend" below for why, and for the load/save functions).

### Screens

Three top-level tabs (`switchTopScreen`, ~line 1614) — Projects, Fixtures, Company — plus two fullscreen overlay modes that aren't tabs: the **Area Editor** (`#editorScreen`, `openAreaEditor`/`closeAreaEditor` ~line 1792) where photo-based design happens, and the **Crop screen** (`#cropScreen`, `openCropScreen` ~line 1844) used after any photo upload/capture.

There's also a **marketing landing page** (`#landingScreen`, top of `<body>`, `showLandingScreen()`/`hideLandingScreen()` near `setAppVisible`) shown to logged-out first-time visitors instead of the sign-in gate appearing immediately — its "Get started free"/"Sign in" buttons hide it and open `#accountModal` in the corresponding mode. A *returning* logged-in visitor never sees it: the `onAuthStateChange` listener resolves to `enterAppForUser()` before the landing page would matter, so it goes straight into the app. `leaveApp()` (fired on logout or no session) and a logged-out `closeAccountModal()`/Cancel both route back to the landing page now — it's the true "nothing to show" resting state, not the auth modal directly.

### Key subsystems (search these section-comment banners, e.g. `// ---------------- wire runs ----------------`, to navigate)

- **Canvas pan/zoom** (~line 894): desktop ctrl+scroll / pinch-to-zoom for precise fixture placement on the photo, independent of the CSS layout.
- **The design canvas (`.editor-canvas`) is always black, regardless of light/dark theme** — it's meant to simulate nighttime, so it deliberately doesn't follow `--ink`/`--surface`. This means any element placed on it (or on the always-dark print background, see the print-background note below) **cannot use theme-dependent color tokens like `var(--fog)`/`var(--line)` for text/borders and assume it'll be readable** — in light theme those resolve to dark colors meant for light surfaces, which nearly disappear against a fixed black background. This bit `.canvas-empty .btn-ghost` ("Choose photo", a real user-reported bug: text computed to `rgb(33,27,18)` on black) — `.btn-primary` elements nearby didn't have the problem because their own solid fill guarantees contrast regardless of theme, but a transparent `.btn-ghost` has nothing to fall back on. If you add new UI to the always-dark canvas (or reuse `.btn-ghost`/similar theme-token-based styles there), hardcode light colors for it rather than relying on the normal theme tokens.
- **Beam rendering** (~line 938, `buildBeamHtml`): computes the on-screen gradient/cone HTML for each placed fixture's light beam from angle, reach, brightness, and Kelvin color temp (`kelvinColor`). This same math is duplicated in Canvas 2D form for export — see `drawBeamOnCanvas` (~line 2270) — because DOM-to-canvas libraries couldn't faithfully reproduce the CSS gradients (see comment at ~line 2239). **The shape is no longer hard-edged — it now has a soft Gaussian-blurred edge (see the blur note further down) — but the underlying hard-edged wedge/circle geometry, opacity curve, and color are otherwise unchanged.** History: a "make it look more realistic" pass with *all three* of screen blend mode + blur + a small hotspot circle was tried early on and explicitly reverted per direct user feedback after seeing it live — at the time, the user preferred the hard-edged look over that softened one, and only wanted the *opacity* turned down (see the opacity note below), not blur/hotspot/blend-mode. **Blend mode was reintroduced later with confirmation (see that note), and blur has now *also* been reintroduced with confirmation** ("make the beam a bit softer on the edges" → offered a choice between a light feather and a full soft blur → user picked full soft blur, explicitly knowing it's close to the direction tried and reverted before). **The hotspot circle is the one part of the original three-part experiment that's still off the table — don't reintroduce it without confirming first.** Don't treat blur as settled/final the way you shouldn't for opacity or blend mode either — same "ask before an even bigger swing, but the current direction is confirmed-so-far" status as those.
  - **The blur is an SVG `<feGaussianBlur>` filter (`stdDeviation` = `BEAM_BLUR_STD`, in the shape's own fixed 400-unit viewBox space, 190 being the far radius), not CSS `filter:blur()` on the whole SVG element** — deliberately, to avoid the exact same "looks right at one specific rendered width, wrong at any other" trap the beam's own responsive sizing and the lit-area reveal were both already fixed for (a CSS-pixel blur radius has no way to scale itself the way an SVG filter's viewBox-relative units do automatically). This is also why the 360° pool/path-light beam changed from a plain CSS `radial-gradient` `<div>` to an SVG `<circle>` — it needed to be SVG to carry the same filter. `.beam-svg` needed `overflow:visible` added, since SVG elements clip to their viewBox by default (unlike ordinary HTML elements) and the blur spreads pixels outward past the shape's own path. The Canvas 2D export path can't use an SVG filter, so `drawBeamOnCanvas` reapplies the same `BEAM_BLUR_STD/190` ratio as a real-pixel `ctx.filter = 'blur(...)'` against that branch's own real-pixel radius — same reasoning as the hardscape base-width ratio conversion right next to it in that function, real canvas pixels standing in for viewBox units since there's no viewBox there to scale it for free.
    - **`BEAM_BLUR_STD` started at 14, was cut to 5, and has now been cut again to 2.5** — the first cut was a real user-reported "looks foggy now" complaint (at 14, the blur was strong enough to dissolve the cone/circle's own directional shape into an ambiguous soft blob rather than just softening its edge); the second came from the same complaint recurring ("looks foggy/blurred") after the opacity boost below made the effect more noticeable overall — see that note for why opacity and blur read as one combined "haziness" impression to a viewer even though they're independent constants. This is a shared constant (used by both SVG filter instances and both canvas branches) specifically so a future retune only has to change one number. Still "confirmed too strong, not yet confirmed correct" — if a future report says it's still foggy, keep pulling this down.
  - **Blend mode specifically has since been reintroduced, deliberately and with confirmation** (unlike blur/hotspot, which have not been revisited): `.beam-svg`/`.pool` both carry `mix-blend-mode:plus-lighter` (and `drawBeamOnCanvas` sets `ctx.globalCompositeOperation='lighter'` to match, for the Canvas 2D JPG-export path) — true additive compositing, clamped at full brightness — so the beam's color adds *directly* onto the photo's own underlying brightness at each pixel instead of sitting as a flat translucent tint. An already-bright leaf highlight or pale surface gets pushed toward a strong, sometimes fully blown-out highlight; a dark gap stays comparatively dark; so the light visibly interacts with the photo's actual texture. This was an explicit user request ("make it recognize leaves/texture, not just a flat triangle on top") after being shown that true scene-aware relighting (recognizing a tree, casting real shadows from occluders) isn't feasible client-side without a depth-estimation model — blend-mode compositing was offered as the achievable middle ground, and the user chose it knowing it's the same *category* of change (blend mode) that was reverted before. **`screen` (the more conservative photo-editing blend mode) was tried first and reverted after the user reported not being able to see any difference at all against a real photo.** Root cause: at this beam's fairly high opacity (see the opacity note above), `screen`'s formula (`1-(1-a)(1-b)`) stays close to plain alpha compositing once the source is already near-opaque — there wasn't enough headroom left for backdrop luminance to visibly show through, so the "texture interaction" was real (verified via direct pixel-math comparison) but too subtle to notice by eye. `overlay`/`soft-light` were ruled out even earlier for a related reason — these scenes are usually quite dark (`photoFilterFor`'s brightness reduction simulates night), and overlay's multiply-dominant behavior on a dark backdrop would make the beam read as *weaker* exactly where it needs to look like it's adding light. Additive (`plus-lighter`/`lighter`) has no ceiling like screen does — it keeps pushing brightness up regardless of how opaque the source already reads — and is arguably the most physically correct of the three anyway (a real light adds photons; it doesn't "screen" or "overlay"). **If a future "I still don't see it" comes up, the opacity/blend-mode tension is the thing to revisit**: this beam is fairly opaque by design (see the opacity note above, a separate confirmed-but-unsettled change), and a near-opaque source layer inherently limits how much of the backdrop *any* blend mode can let through — turning opacity down somewhat (at least in the outer/dimmer part of the gradient) would let more texture interaction show, at some cost to the "vivid" look the opacity increase was for. Those two goals pull in opposite directions; don't try to solve it by escalating the blend mode again if that tension is the actual bottleneck. Like the opacity change above, this hasn't been confirmed as a keeper yet — the user hasn't seen it live and said which they prefer, so there's an annotated git tag `pre-beam-blend-mode` at the commit right before blend mode was first introduced (`screen`, since superseded by `plus-lighter`) as a safe return point back to the flat-tint version. If it needs reverting instead by hand, `mix-blend-mode` on both `.beam-svg`/`.pool` plus the `ctx.globalCompositeOperation` line in `drawBeamOnCanvas` are the only three places it touches. **Update: `plus-lighter` was confirmed liked.** The predicted "opacity/blend-mode tension" above did bite, but from a different direction than expected — not the beam's own opacity, but `photoFilterFor`'s Darkness slider being fixed (see below) to actually reach near-black. Once the ambient photo has almost no luminance left at all, *no* blend mode has any backdrop signal left to interact with, so the beam read as a flat pale shape again regardless of blend choice. Turning the beam's own opacity down (the fix predicted above) wouldn't have addressed that — see the lit-area-reveal note below for what actually did.
  - **The "flashlight" lit-area reveal** (`buildLitAreaPath`, `.photo-lit`, `litPhotoFilterFor`): a second, much-brighter copy of the area photo is stacked directly on top of the darkened one and clipped to exactly the union of every placed beam's cone/circle shape, so the areas a fixture is actually lighting up show the real photo (leaves, bark, texture) at near-full brightness, while everywhere outside a beam stays whatever the Darkness slider set — "like shining a flashlight" onto the real scene, per the user's own framing, rather than tinting a translucent shape over a near-black photo. This became necessary specifically because of the Darkness fix below: once 100% darkness reads as near-black, the beam's additive blend mode (previous note) has almost no backdrop luminance left to visibly interact with, so a purely color/blend-based beam reads as a flat pale wash again regardless of which blend mode is used — revealing the *actual* photo pixels underneath, rather than trying to synthesize the look via compositing math alone, is what actually fixes that.
    - **The reveal is now an inline SVG `<mask>` with a Gaussian-blurred, alpha-union mask — NOT a `clip-path` + CSS `mask-image` (this supersedes every bullet below that mentions `clip-path`/`clipPath`/`buildLitFadeMaskImage`/`#litclipPath-editor`; the *reasoning* in them — objectBoundingBox fractions, `aspect` compensation, print robustness, `LIT_FADE_STOPS` tuning — all still applies).** Second real report on the same "clustered downlights don't feather" complaint, after the `.beam-group` fix (see above) only fixed the tint: the *brightened photo patch* still ended on a hard clip-path edge, and a cluster's overlapping radial-fade masks stacked (3 × 0.35 → ~0.73 alpha) right along that hard edge, so a lone fixture's faint edge became a crisp bright polygon. Only visible against a bright, realistic photo — the earlier dark synthetic test photo hid it entirely, so **test reveal/feathering changes on a bright daylight-ish photo, not a dark one**. Fix: `.photo-lit` is now `<svg class="photo-lit"><defs><mask maskContentUnits="objectBoundingBox">…</mask></defs><image href=photo preserveAspectRatio="xMidYMid slice" mask=…/></svg>` (editor: `#photoLitSvg`/`#photoImgLit` is now an SVG `<image>`, set via `setAttribute('href', …)`, CSS `filter` lives on the `<svg>` root; static thumbnails: one per area card in `renderAreaList`). `buildLitMaskShapes(area, aspect, idPrefix)` emits, per placement, that placement's own `buildLitAreaPath` shape filled with a radial gradient of `LIT_FADE_STOPS` (y radius scaled by `aspect` via `gradientTransform`) and blurred by `LIT_EDGE_BLUR_STD` (9, same 190-unit space as `BEAM_BLUR_STD` but deliberately much bigger — the tint stays crisp-ish, the lit *photo* edge is soft; y std scaled by `aspect` too). Shapes composite source-over inside the mask = alpha union, so overlap saturates smoothly. `updateLitReveal` just rewrites `#litMaskShapes-editor`'s innerHTML (cheap enough per pointermove). The Canvas JPG export builds the same thing (blurred radial-fade shapes into an alpha `maskCanvas`, bright photo on its own `litCanvas`, `destination-in` the mask onto *that*, then composite) — this also fixed a latent export bug where the old per-placement `destination-in` ran on the MAIN canvas and multiplied away the dark photo beneath every beam (semi-transparent → black in the JPG). If reveal edges ever read too soft/hard, `LIT_EDGE_BLUR_STD` is the one knob. Verified against a bright synthetic photo (before: hard diagonal reveal edges around a 3-fixture cluster; after: smooth glow), mid-drag mask updates, the thumbnail, and a real JPG export.
    - **`updateLitReveal(area)`** wraps the two lines (`#litclipPath-editor`'s `d` + `#photoImgLit`'s `mask-image`) that keep this reveal in sync with the current placements, and has to be called from every place that moves/re-aims/re-widens a placement live during a drag — not just once at the end. It wasn't, originally: `attachPlacementHandlers()`'s three drag handlers (position dot, aim handle, width handle) each called `updatePlacementBeam()` (or just moved the wrapper's `left`/`top`) on every `pointermove`, correctly moving the beam SVG itself in real time, but the reveal stayed frozen at its pre-drag shape until `renderOverlays()` ran again on drag-end. Real, screenshot-confirmed bug ("looks good when placed, then changes when you move it"): for the whole duration of a drag, the tinted beam visibly separated from the brightened photo patch it's supposed to sit over — both the new (dim, unrevealed) and old (still-lit, now-orphaned) positions were visible at once, a ghost-duplicate look. Root-caused by dispatching synthetic `pointerdown`/`pointermove` events mid-drag (no `pointerup`) and screenshotting that exact frame — confirmed it self-corrected on release, which is what made it easy to miss in casual testing but very noticeable during actual real-time dragging. Fixed by calling `updateLitReveal(area)` from all three `onMove` handlers, the same way `updatePlacementBeam()` already was — the fix is purely additive (one function call each), no change to `buildLitAreaPath`/`buildLitFadeMaskImage` themselves. If a new drag-capable interaction is ever added to a placement, it needs this same call or the reveal will go stale for it too.
    - **Present in all three beam-rendering pipelines**, each with its own `<img>`/canvas draw of the same brighter photo clipped to the same shape: the live editor (`#photoImgLit`, updated every `renderOverlays()` call), the static/print area-card thumbnail (`.photo-lit`, baked directly into `renderAreaList`'s template string — no separate refresh call needed, unlike the beam overlay, since it needs no DOM measurement at all), and the Canvas 2D JPG-export path (`captureAreaDesignImage`, via `ctx.clip(new Path2D(...))`).
    - **The clip shape is built as SVG `objectBoundingBox` fractions (0–1 per axis), not the CSS `clip-path:path(...)` function** — that distinction matters and was a real mistake caught mid-implementation: `path()` always interprets its numbers as raw pixels of the target's own box (there's no percentage/fraction equivalent), which would have reintroduced the *exact* print-reflow staleness bug that the beam's own sizing (see the separate CLAUDE.md note on that) was fixed for — a container can render at a different width in print than it did on screen, and pixel coordinates frozen at generation time don't track that. `objectBoundingBox` fractions have no such problem: the browser reapplies them against whatever the real box turns out to be, automatically, at any time — so, like the beam's own percentage sizing, this needs no `beforeprint`-style re-measurement hook (that approach was already tried once for the beam and found unreliable for real print/PDF pagination — see that note).
    - **`objectBoundingBox` scales x by the box's real width and y by its real height *independently*, which distorts a naively-uniform circle/cone into an ellipse unless compensated for.** `buildLitAreaPath` handles this by multiplying every y-going radius/offset by `aspect` (`area.photoAspect`, width/height) and leaving every x-going one alone — verified visually by placing a 360° path-light fixture and confirming its reveal renders as a true circle, not an ellipse, and by dragging a cone fixture's aim/width handles and confirming the reveal shape tracks rotation and beam-width changes exactly. `aspect` has to be passed in explicitly since fractional coordinates alone can't reveal what the real box's aspect ratio is the way pixel coordinates would.
    - **`litPhotoFilterFor()` is deliberately not tied to `area.darkness`** — fixed at `brightness(0.95)` regardless of the Darkness slider, since what a fixture's light reveals should look about the same whether the ambient scene is set dim or pitch-black, the same way a real flashlight's beam doesn't get dimmer just because the room around it got darker. Only the *shape* it's clipped to depends on the design (reach, beam width, rotation) — never its own brightness.
    - **The clip-path alone gives a hard-edged reveal — real light doesn't hit an invisible wall at its reach distance, it just gradually dims** (a real user-reported "looks kinda weird" specifically about the far edge, right after the reveal itself first shipped). Fixed by layering a `mask-image` on *top of* the existing clip-path (both apply simultaneously; the clip-path still supplies the correct angular/directional boundary — a fixture's beam width being fairly sharply defined is realistic on its own) — `buildLitFadeMaskImage` builds one `radial-gradient(ellipse Xpct% Ypct% at xPct% yPct%, white→transparent)` per placement (CSS's own percentage-of-element-box sizing, so it needs the same `aspect` compensation as `buildLitAreaPath` and is exactly as print/resize-robust), comma-joined so multiple placements' fades union via the CSS default `mask-composite: add`. The Canvas 2D export path can't share this directly (canvas has no multi-layer `mask-image` equivalent), so it's done per-placement instead: clip to that one placement's shape, `drawImage` the bright photo, then `ctx.globalCompositeOperation='destination-in'` with a matching `createRadialGradient` fade — each placement gets its own `save()`/`restore()` specifically because `destination-in` multiplies the *entire* existing canvas alpha, so doing it once per placement over a shared drawImage would have each fixture's falloff erase every other fixture's already-faded area instead of just its own.
    - **`LIT_FADE_STOPS`' peak has been tuned down twice since it first shipped, both times from real user feedback that the base still read too bright/blown-out** (`[0,1],[45,.8],[75,.25],[100,0]` → `[0,.4],[20,.65],[45,.55],[75,.15],[100,0]` → the current `[0,.15],[20,.35],[45,.3],[75,.08],[100,0]`) — the combination of a near-fully-revealed photo *and* the beam's own additive color (also strongest near the fixture) compounds fast, so it takes a much lower peak than intuition suggests to read as "soft" once both layers stack. If a future report says it's still too bright, keep pulling the whole curve down rather than assuming this settled at two rounds — there's no indication it's been confirmed correct yet, only that each prior value was confirmed *too high*.
  - **Beam color comes from `beamKelvinColor(k)`, NOT `kelvinColor(k)`.** User: "color temperature slider doesn't work" right after the tint was cut for the haze fix. The slider handler itself was fine (verified: `p.kelvin` updates and the gradient `stop-color` changes) — the problem was that `kelvinColor()` is a 4-bucket ramp of very *pale* colors (`255,223,188` … `237,244,255`), and once the tint's opacity was halved that difference became imperceptible. `beamKelvinColor` is a smooth piecewise ramp (`BEAM_KELVIN_STOPS`: 2200K amber `255,138,38` → 2700K `255,166,80` → 3000K → 4000K → 5000K cool `232,238,255`) used only by `buildBeamHtml` and `drawBeamOnCanvas`; strips/dots still use `kelvinColor` (untouched on purpose). It has to be *saturated* to work: a low-alpha additive tint only reads as a color cast if the color is saturated — a pale one just adds grey (= the haze). Verified at 2200K (clear amber) vs 5000K (cool white). Side effect: a default 2700K beam is now a warmer amber than before.
  - **LATEST (supersedes the opacity/`LIT_FADE_STOPS` values in the CURRENT STATE bullet below): user sent a screenshot and said "almost like you are adding a white haze on it."** The haze was never the edges — it's the colored *tint* layer: additive `plus-lighter` of a pale warm color onto a dark/mid photo lifts the blacks and flattens contrast inside the whole beam (this is also the original session-start "foggy" complaint, so it predates all the feathering work). Fix is to shift the beam's lighting work off the tint and onto the reveal, which is the REAL photo brightened (keeps leaf/bark detail and contrast, adds no veil): tint opacities cut to cone `0.32/0.16` (sel `0.40/0.22`) / pool `0.28/0.13` (sel `0.34/0.18`) in all four places, and `LIT_FADE_STOPS` raised to `[[0,.3],[20,.7],[45,.6],[75,.2],[100,0]]`. A/B'd on the same seeded foliage scene before/after: before = milky pale cone fill, after = the actual leaves visibly lit with detail. Also edges trimmed: `BEAM_WEDGES = [1,.96]`, `LIT_EDGE_BLUR_STD = 1.2`. **Rule of thumb: brightness comes from the reveal, the tint is only a warm color cast — raising tint opacity reads as haze, so don't.** If beams read too dim, raise `LIT_FADE_STOPS` before touching tint opacity.
  - **CURRENT STATE (values partly superseded by the LATEST bullet above): the user then said "THEY NEED TO BE LIKE THEY WERE BEFORE THESE CHANGES BUT KEEPING THE BLENDING. STILL FOGGY."** So the single-beam look is restored to the original, pre-feathering one, and only the edges are lightly softened: opacities back to cone `0.56/0.27` (sel `0.66/0.35`) / pool `0.46/0.21` (sel `0.56/0.28`), `LIT_FADE_STOPS` back to `[[0,.15],[20,.35],[45,.3],[75,.08],[100,0]]`, `BEAM_WEDGES = [1, 0.96]` (only the outer ~4% of the half-width feathers), `LIT_EDGE_BLUR_STD = 1.2` (both trimmed once more after "looks better but still foggy"). "Blending" = the `.beam-group` alpha-union + reveal-mask union (overlapping beams don't sum past 1.0), which stays. Verified by running the original build (`git show f56b3f9:index.html`, served on a second port) and the current one against an identical seeded test scene and comparing screenshots at the same zoom: beam brightness/definition matches, edges are the only difference. **Lesson: every time "more/wider/softer feathering" was tried (wedges `[1,.78,.56,.34]`, reveal blur 5–9, raised opacities/reveal), the user read it as fog and dimness — the interior of the beam must stay exactly as originally tuned; only tune the edge band, and compare against the original build side by side before shipping.** If edges read too hard, widen `BEAM_WEDGES` slightly or raise `LIT_EDGE_BLUR_STD` in small steps; don't touch opacities/`LIT_FADE_STOPS`.
  - **Retune after the feathering work (user, on the real `dev` build): "no hard edge, but the beams are not bright enough and look foggy."** *(Values below were tried and then reverted — see CURRENT STATE above.)* (Earlier "same issue" screenshots were from the wrong account/`main` build — always confirm which build a screenshot is from before re-diagnosing.) Two causes: the feathering itself dimmed/hazed the beam (`BEAM_WEDGES` was `[1,.78,.56,.34]` — a narrow bright core wrapped in a wide dim haze; `LIT_EDGE_BLUR_STD` 9 spread the brightened-photo patch into a wide veil), and the per-beam opacities were the pre-alpha-union dimmer values. Now: `BEAM_WEDGES = [1,.9,.8,.7]` (only the outer ~30% of the half-width feathers), `LIT_EDGE_BLUR_STD = 5`, `LIT_FADE_STOPS` peak raised to `[[0,.25],[20,.6],[45,.55],[75,.2],[100,0]]` (the reveal is the *real photo* brightened, so it adds brightness AND detail rather than haze — shift brightness there rather than into the tint), and beam opacities raised to cone `0.64/0.31` (sel `0.74/0.39`), pool `0.52/0.25` (sel `0.62/0.32`) in all four places. Raising opacity is safe now in a way it wasn't before: overlaps composite by alpha union inside `.beam-group` instead of summing past 1.0, which is exactly why the earlier boost blew out. Judged only against a synthetic photo — if it's still dim or foggy on a real one, tune `LIT_FADE_STOPS` first, then opacities; if edges get hard again, widen `BEAM_WEDGES`.
  - **A cone's SIDES are now feathered too (`BEAM_WEDGES`/`wedgeFalloffStops`).** Third report of "beams don't feather into the photo" (with a screenshot of two wide uplights on a dark autumn photo): the cone had a razor-sharp straight edge along both sides and the far arc — the radial falloff only faded it along its *length*, and `BEAM_BLUR_STD` (2.5 ≈ 1.3% of the radius, ~4px) is far too small to soften a side edge, so it read as a flat cut-out. Neither the `.beam-group` fix nor the reveal-mask blur touched this. Since SVG has no conic gradient, the cone is drawn as 4 nested wedges (`BEAM_WEDGES = [1,.78,.56,.34]` × half-width), all sharing one radial gradient whose stop opacities are remapped to `1-(1-op)^(1/4)` so that on the axis the stacked wedges add back to exactly the old opacity (on-axis brightness unchanged) while the outer edge is covered by only the widest wedge (~1/3 as opaque) — brightest down the axis, dimming toward the sides, then the existing blur smooths the steps. Same in `drawBeamOnCanvas`. Pool/360° beams have no sides so are untouched. **The earlier "2 vs 3-4 overlapping beams" and reveal-edge work were real but were not what this screenshot showed — when a user says "same issue" with a screenshot, find what in *that* screenshot is actually hard-edged before assuming the previous diagnosis covers it** (I wrongly told the user it was fixed twice). Verified at wide/long reach (before: crisp vertical side edge; after: soft), medium reach, the thumbnail and a real JPG export. `BEAM_BLUR_STD` is still 2.5 — if sides still read hard, raise it modestly (it was 14 → "foggy"), or add wedges.
  - **All of an area's beams share ONE isolated `.beam-group` layer; that group (not each `.beam-svg`) carries `mix-blend-mode:plus-lighter`.** Real user report: placing several downlights in one spot made the beams stop feathering into the photo (flat/hard/washed-out where they cluster) — the user's own hypothesis ("because we made two lights on one spot brighter") was correct. Root cause: each beam used to be additively blended onto the photo *separately*, so N stacked beams' alphas summed with no ceiling (4 × ~0.56 ≈ 2.2, clipped at 1) and any gradient/texture under that sum was flattened. The 2-beam case was mostly solved earlier by the opacity revert below; 3–4 clustered downlights (a normal real-world setup) still clipped. Fix: beams composite *with each other* via plain source-over inside the isolated group (alpha union `1-(1-a)(1-b)…` — saturates smoothly, never clips), and only that combined layer is added onto the photo. A single beam looks identical to before; overlaps still get brighter but level off. Structure: `renderOverlays()`/`buildStaticOverlaysHtml()` emit `<div class="beam-group">` (first child of `#overlays`/`.overlays`) holding one `.beam-anchor[data-uid]` (positioned at the fixture's `left/top %`, wrapping `.beam-layer`) per placement, followed by the normal `.placement` wrappers that now hold only dot/handles/label — so beams also sit under *all* dots. **Anything that moves/re-renders a beam must target `.beam-anchor`, not `.placement`**: `updatePlacementBeam` swaps the anchor's `.beam-layer` html, and the position-dot drag handler updates both the `.placement` and the `.beam-anchor` `left/top`. Static/print needs `.area-card-photo .overlays .beam-anchor{width:100%;height:100%}` for the same percentage-sizing reason as `.placement`. Canvas JPG export mirrors it: `captureAreaDesignImage` draws every beam (source-over, `drawBeamOnCanvas` no longer sets `'lighter'`) onto a transparent offscreen `beamCanvas`, then `drawImage`s that onto the main canvas with `'lighter'`. Verified live on `dev` with a 4-fixture stack (A/B against the old per-beam blend via an injected style override: old = blown-out flat core, new = soft gradient with leaf texture still visible), a mid-drag frame (beam + lit reveal move together, no ghost), the static thumbnail, and a real JPG export.
  - **Opacity history — the boost was tried and reverted; back to the dimmer, pre-boost values.** The very first version's opacity was too high, masking the photo's own detail (leaf/branch texture) instead of sitting over it translucently — a real user comparison against an actual finished-lighting photo made this obvious, so it was dropped ~30% from those original values (cone: unselected `o1=0.56`/`o2=0.27`, selected `o1=0.66`/`o2=0.35`; pool: unselected `o1=0.46`/`o2=0.21`, selected `o1=0.56`/`o2=0.28`). That lower-opacity version shipped and was confirmed correct at the time. It was later bumped back up to the original (pre-dimming) values as an explicit, tentative request to make beams more vivid — with an annotated git tag `pre-beam-opacity-boost` left as a safe return point specifically because that boost hadn't been confirmed. **It's since been reverted via that exact tag**, prompted by real feedback after seeing it live under the additive `plus-lighter` blend mode (see that note): at the boosted opacity, two overlapping beams' colors add together fast enough to blow out to a washed-out/foggy look, which is exactly the "foggy, blurred, and way too bright when overlapping" complaint that came back. The current values are the dimmer pre-boost ones listed above, in all four places this constant pair appears (`buildBeamHtml`'s pool/cone branches and `drawBeamOnCanvas`'s pool/cone branches — the canvas-export path only ever used the "unselected" numbers). If vividness is requested again, remember this specific failure mode (additive blend mode has no ceiling, so raising per-beam opacity multiplies badly wherever two beams overlap) rather than just re-applying the old boosted numbers unchanged.
- **Fixture-type identity colors** (`FIXTURE_TYPES`/`fixtureTypeMeta`, near `kelvinColor`): each fixture has a `type` field (`accent`/`path`/`inground`/`flood`/`downlight`/`hardscape`/`other`) distinct from `kelvinColor` — `kelvinColor` is the beam's actual light-temperature color, `FIXTURE_TYPES` is a fixed identity color per fixture *type* used purely so a placement dot, its row in the materials list, and the legend all agree on "which color means what," regardless of what color that fixture actually emits. Colors are set inline per-instance (not hardcoded in CSS) at every place a fixture shows up: the canvas marker-dot (`renderOverlays`), the static/print place-dot (`buildStaticOverlaysHtml`), the canvas-export dot (`drawDotAndLabelOnCanvas`), the materials/quote table rows and fixture-picker/fixture-library lists (`quoteItemNameCell`, reused across `renderProjectQuote`/`renderSheetList`/`renderFixtureMgmtList`), and two legends (`usedFixtureTypes`+`buildTypeLegendHtml`) — a floating one over the design canvas (`#editorLegend`, scoped to the current area's placements) and one in the project detail "Areas" section (`#areaTypeLegend`, scoped to the whole project, which also prints since that section isn't print-only). Both legends only list types actually in use, not the full fixed set, to avoid clutter on a design that only uses one or two types. **In print, `.place-dot`'s CSS forces `background:#fff` for contrast against any photo — the inline per-type `border-color` still wins over that class rule** (inline beats class regardless of media query), so it prints as "white fill, type-colored ring," same identity color as on-screen; don't try to also force border-color in that print rule, it would silently defeat the whole feature there. Custom fixtures and base-fixture overrides created before this feature don't have a `type` — `fixtureTypeMeta` falls back to `other` (grey), consistent with how other schema-less-JSONB fixture fields degrade elsewhere in this file.
- **"Hide prices on customer quote (export a material list only)" toggle** (`#toggleHidePricing`, `renderProjectQuote`'s customer-mode branch): drops the Price/Total columns and the entire labor/subtotal/discount/tax/total footer, leaving just Item + Qty — for handing a customer or supplier a plain scope-of-work/material list with no dollar amounts, using the exact same "Print / Export project" flow as a priced quote (open the Customer tab, check this box, print). Deliberately scoped to the customer-mode table only, same as `toggleQuoteFixtureType` right below it — Internal mode always shows cost/price/margin regardless, since hiding pricing there wouldn't make sense. Composes with the existing "show fixture type" toggle (still swaps the item cell) since it only touches the columns after that, and needs no schema/mapper change (a plain checkbox re-render, not persisted state). Purchase orders and the on-screen labor/discount inputs are unaffected (POs already never print at all; the inputs just stop mattering while pricing is hidden). Verified live: toggled on/off against a real priced item, table dropped to two columns with no total row and reverted correctly.
- **Customer-quote "Show fixture type instead of name/description" toggle** (`#toggleQuoteFixtureType`, `quoteItemNameCell(i, preferType)`): when on, this swaps out whatever's shown for the item's **name** on the customer-facing quote table only — internal-mode quotes and every other `quoteItemNameCell` caller (`renderSheetList`, `renderFixtureMgmtList`) always pass no `preferType` and are unaffected. The substitute differs by category since fixtures and accessories don't have the same concept of "type": a fixture (`cat:'fixture'`) shows its `FIXTURE_TYPES` label + identity-color dot (see the note above) instead of its specific name/part number; an accessory (`cat:'accessory'`, no `type` field) shows its own `desc` field instead, since description is the customer-friendly stand-in for whatever part-number-ish text ended up in `name` after import. Strips are untouched either way — not asked for. The description-suppression logic in `renderProjectQuote`'s customer-mode branch (`${showType ? '' : cell-sub}`) needed no change to support this: it was already gated on the toggle itself rather than on fixture-vs-accessory, so it already stops the sub-line description from duplicating the accessory's now-promoted-to-main-cell description.
- **Hardscape fixtures** (`f10` in `BASE_FIXTURES`, `type:'hardscape'`): a 6" linear bar fixture for steps/walls/paver joints, added as a regular catalog `cat:'fixture'` entry with its own identity color and specs matching a real hardscape light's subtle output (wide-ish `beam:100`, low `lumens:90`/`watt:2`). Unlike every other fixture type, it gets two deliberately different visuals, per direct user feedback that the initial version (reusing the standard point-source rendering as-is) didn't read as a linear source:
  - **Marker is a rotated bar, not the round dot every other fixture uses** — `.marker-dot.is-bar`/`.place-dot.is-bar` (smaller width/height + less rounding than the base class) plus an inline `transform:translate(-50%,-50%) rotate(${p.rotation}deg)` applied only for `f.type==='hardscape'`, in all three render paths (`renderOverlays`, `buildStaticOverlaysHtml`, `drawDotAndLabelOnCanvas`). The bar's un-rotated default orientation (wider than tall) is already perpendicular to "up" (`p.rotation===0`), so `rotate(p.rotation)` alone keeps it perpendicular to wherever the beam is aimed — no extra +90° offset needed. Note the `.just-placed` pop animation's own `transform` keyframes momentarily override this inline rotation for its ~0.5s duration (CSS animations win over inline styles in the cascade); the bar snaps back to its correct rotation once the animation ends and the class is gone on the next render. Harmless, not worth fixing.
  - **Beam has a flat trapezoid base instead of converging to a point** — a real 6"+ LED bar doesn't emit from a single point the way a spot fixture's lens does. `HARDSCAPE_BASE` (=22, a half-width in the same fixed 400-unit space `buildBeamHtml`'s far radius of 190 already uses) defines two extra path points at ±90° from center, so the SVG path becomes `base-corner → far-arc-start → arc → far-arc-end → other-base-corner → close` instead of `center → far-arc-start → arc → far-arc-end → close`. `drawBeamOnCanvas`'s canvas-export equivalent has to re-derive the same 22/190 ratio by hand against the real pixel radius `R`, since canvas drawing has no viewBox to scale the fixed-unit-space math for free the way the SVG version does.
- **Wire runs & LED strips** (~lines 1017, 1039): freehand paths drawn on the photo, stored as point arrays, rendered as SVG overlays live and re-drawn onto canvas for export.
- **Drag/placement handlers** (~line 1192, `makeDraggable`; ~line 1216, `attachPlacementHandlers`): pointer-event-based dragging for moving fixtures, aiming beams (ring handle), and widening beams (square handle).
- **Undo/redo** (section banner `// ---------------- undo/redo (area editor) ----------------`, near the other module-level editor state): snapshot-based, not a command pattern — `pushUndoSnapshot()` JSON-stringifies the current area's `placements`/`wireRuns`/`lightStrips`/`accessoryQty`/`darkness`/`beamBrightness` onto `undoStack` (capped at `UNDO_LIMIT`, 50) *before* a mutation happens, and `undoDesign()`/`redoDesign()` pop/push between `undoStack` and `redoStack`, restoring via `applySnapshot()`. Scoped to whichever area is currently open — both stacks are reset in `openAreaEditor()`, so undo history never carries across areas or projects. A fresh action (anything that calls `pushUndoSnapshot()`) always clears `redoStack`, matching standard undo/redo semantics — once you make a new change, the old "future" is gone. JSON-stringifying is deliberate: it makes each stack entry a real independent copy, so later mutating the live area object can never retroactively corrupt a snapshot already on the stack (a plain object reference would not have this guarantee).
  - **Every mutation site needs its own `pushUndoSnapshot()` call — there's no central interceptor.** Since this isn't a proxy/observer over `state`, adding a new way to mutate an area's design (a new tool, a new modal, a new stepper) means remembering to add the call yourself at that exact call site, ideally as the first line after validating the mutation will actually happen. Current call sites: placing a fixture (`handleCanvasClick`), removing/duplicating one (`infoRemove`/`duplicatePlacement`), dragging a fixture/beam-aim/beam-width handle (`makeDraggable`'s `move()`, only once the 4px drag threshold is crossed — not on every `pointermove`, and not on a plain click that never becomes a drag), saving or deleting a wire run or LED strip (`saveWireModal`/`deleteWireRun`/`saveStripModal`/`deleteLightStrip`), accessory qty +/- (the `[data-inc]`/`[data-dec]` handlers in `renderSheetList`), and "Clear design". The four range sliders that mutate area/placement state continuously while dragged (`ctrlDarkness`, `ctrlBeamBrightness`, `infoKelvin`, `infoBrightness`) snapshot on `pointerdown` rather than on every `input` event, so a whole drag of the slider is one undo step instead of dozens — the same one-snapshot-per-gesture idea as the drag handles, just triggered by `pointerdown` instead of a distance threshold since there's no separate "click vs. drag" distinction to make for a slider.
  - **`.edit-panel` (the shared base class for every bottom-sheet modal — customer, fixture form, area name, distributor, and the per-fixture info sheet with `infoKelvin`/`infoBrightness`) needs `user-select:none`, or dragging a range slider inside one only works some of the time — a real user-reported "sliders rarely work" bug.** Root cause, confirmed directly (not just suspected): a drag gesture that doesn't land in the exact pixel-perfect hit-region of the thumb falls through to the browser's native text-selection drag instead, since nothing was suppressing it — `window.getSelection().toString()` after a failed drag returned a real non-empty fragment of the panel's own hint text, proving the browser had claimed the gesture as a text selection instead of passing it to the slider. This is the *same* root cause as the documented wire-run/photo-selection bug on the design canvas (`user-select:none` + `preventDefault()` needed together there) — just never applied to this other, unrelated place a drag can happen. The event-handler *logic* itself was verified correct independently (setting `.value` and dispatching a synthetic `input` event always worked) — the bug was purely about the gesture never reaching the slider at all, not about what happens once it does. `user-select:none` doesn't affect an `<input>`'s own internal text editing/selection (that's standard browser behavior, unaffected by an ancestor's `user-select`), so this doesn't stop typing in e.g. the customer name field, which shares the same `.edit-panel` class — verified directly. Deliberately not `touch-action:none` on the same rule — that would also block touch-scrolling the panel's own content on mobile (`.edit-panel` has `overflow-y:auto;max-height:90vh`), which `user-select:none` doesn't touch.
  - **Ctrl+Z/Cmd+Z undoes, Ctrl+Shift+Z or Ctrl+Y redoes** — a single `keydown` listener on `document`, gated on the editor screen being open (`!$('editorScreen').hidden`) and on focus not currently being in a text-like field (`<textarea>`, `contenteditable`, or an `<input>` whose `type` isn't `range`/`checkbox`/`radio`) so it doesn't fight a browser text-undo the user is actually trying to do in, say, the customer-name field elsewhere in the app. Range sliders are deliberately exempted from that text-field check — they have no native undo of their own, so Ctrl+Z should still reach the app's undo while a slider has focus.
  - **`#btnUndo`/`#btnRedo` disable via the plain `disabled` attribute, not `hidden`** — unlike the fixture-type legend/toolbar/banner elsewhere in this file, there's no `[hidden]`-vs-author-`display` gotcha here since `:disabled` is a real interactive-element state the browser itself enforces, not a display toggle fighting a class rule.
- **Drawing a wire run or LED strip (drag across the photo) needs BOTH a JS `preventDefault()` on the pointerdown/pointermove handlers AND `user-select:none` on `.editor-canvas` — one alone isn't enough.** A real user-reported bug: the photo would flash blue mid-drag, like a text/image-selection highlight. Root cause: the browser's native "drag across the page = select text/images" gesture was never suppressed. `preventDefault()` on `#canvasPhoto`'s pointerdown/pointermove (added for the wire/strip/pan/pinch drags alike) covers drags that start on the photo itself, but the photo doesn't always fill `.editor-canvas` (aspect-ratio letterboxing can leave black margin on the sides) — a drag starting in that margin never reaches a handler that calls `preventDefault()`, so it still triggers a selection, which still visually reads as "the canvas flashed blue" since the margin is the same black as the photo backdrop. `user-select:none` on `.editor-canvas` (not just `#canvasPhoto`) closes that gap. Verified by driving real OS-level mouse drags (not synthetic dispatched events, which don't reliably trigger the browser's native selection gesture the same way) both on the photo and in the letterboxed margin, and checking `window.getSelection().rangeCount` before/after each — confirmed 0 in both cases after the fix, 1 (a real non-collapsed selection) in each case before it.
- **Any element toggled via the `hidden` DOM property/attribute must NOT also get its `display` set unconditionally by a plain class selector — that class rule (an author style) silently wins over the browser's built-in `[hidden]{display:none}` default (a user-agent style), regardless of the `hidden` attribute actually being set.** This is why `.edit-sheet`, `.editor-screen`, and `.crop-screen` each have a matching `.foo[hidden]{display:none;}` rule right next to their base rule — every one of them sets `display` unconditionally (`flex`) and needs the explicit override to make `hidden` actually work. **`.placement-banner`, `.canvas-toolbar`, and `.type-legend` were missing this same override** — a real user-reported bug (hitting "Done" after placing a fixture didn't make the banner go away) traced back to exactly this: `banner.hidden = true` was being set correctly, `updatePlacementBanner()`'s logic was correct, but the element's actual computed `display` stayed `flex` regardless, so the banner (with its now-stale "N placed" text) just sat there. Verified directly via `getComputedStyle(el).display` before/after the fix (not just visually) — was `"flex"` despite `el.hidden===true` before, `"none"` after, for all three elements. **If you add a new absolutely-positioned overlay to the editor canvas (or anywhere else) that sets its own `display` and gets toggled via `.hidden`, it needs this same `[hidden]` override — the bug is invisible in code review since `updatePlacementBanner()`/equivalent logic looks completely correct on its own; it only shows up as computed style.**
- **Quote math** (~line 1483): `computeAreaLineItems`/`computeProjectLineItems` roll up placements + strips + accessories into priced line items; toggled between "Customer" (sell price) and "Internal" (cost) modes.
- **`#projQuoteTable`'s subtotal/discount/tax/total block (`#projQuoteTfoot`) is a plain `<tbody>`, not a `<tfoot>` — this is deliberate, not an oversight.** Browsers automatically repeat `<thead>`/`<tfoot>` on *every* printed page a table spans across; for a real `<tfoot>` holding the totals, that meant a long item list printed the subtotal/discount/tax/total rows once per page instead of once at the true end — a real user-reported bug that looked like two separate partial quotes. `<tbody>` never gets that auto-repeat treatment, so switching the tag fixes it (both the "Customer" and "Internal"/cost-mode footers in `renderProjectQuote` populate the same `#projQuoteTfoot` element regardless) while leaving `#projQuoteThead` as a real `<thead>` — repeating the *column headers* on each page is what you actually want for a multi-page table. Don't "fix" `#projQuoteTfoot` back to a semantic `<tfoot>` without re-solving this.
- **Fixture library + Excel import** (~lines 1885–2119): SheetJS (`xlsx.full.min.js`, CDN) reads an uploaded spreadsheet; `findFixtureHeaderRow`/`parseFixtureRows` locate and parse fixture rows against a downloadable template (`downloadFixtureTemplate`).
  - **The template is a real `.xlsx` binary embedded as a base64 string (`FIXTURE_TEMPLATE_B64`), not generated on the fly** — there's no server and no xlsx-writing library loaded (SheetJS is only used for *reading* an import), so the actual approach is: hand-build/edit the real `.xlsx` as a zip of OOXML parts (this file has no sharedStrings.xml — cells use inline `t="inlineStr"`, so there's no separate strings table to keep in sync when editing rows), then base64-encode the whole file and paste it in as the JS constant. **If you ever need to change the template's columns/wording again, don't hand-edit the base64 — decode it, edit `xl/worksheets/sheet1.xml` (and `<cols>`/`<dimension>`/`<mergeCells>` refs if the column *count* changes), re-zip, re-encode, and verify by feeding the result back through `XLSX.read()` (the exact library the app uses for real imports) before committing.** `parseFixtureRows`'s column indices and this template's columns must stay in lockstep; changing one without the other silently misreads every column after the change point.
  - **Current column order (A–N): Category, Fixture Type, Part Number, Distributor, Description, Your Cost, Sell Price, Beam Angle, Color Temp, Lumens, Wattage, Install Time, Transformer Capacity (W), Voltage Taps** — (M/N were added for transformers and are read only on `Category = Transformer` rows: `parseFixtureRows` takes `row[12]` as `capacityW` — falling back to the Wattage column for a sheet filled in before M existed — and splits `row[13]` on any non-digit/non-dot run into `taps`, blank/unparseable → `[12,13,14,15]`, same default as the fixture form. Category matching is `startsWith`: `access…`→accessory, `transf…`→transformer, `strip…`→strip, else fixture — **then** any row that isn't Fixture-labelled or a Strip and mentions "transformer" in its category, type, part number or description is promoted to a transformer (real user report: an uploaded distributor sheet filed transformers under Accessory, so they landed in the Accessories tab), and a blank capacity falls back to Wattage, then to a `300W`-style number in the name/description. The template's example rows are now Fixture / Transformer (TRF-300W, 300 W, taps 12,13,14,15) / Strip / Accessory (wire spool); merges/dimension/`<cols>` were extended to N. Rebuilt per the decode→edit `sheet1.xml`→re-zip→re-encode rule and verified by `XLSX.read()` plus running the real `parseFixtureRows` source against the template rows.) The original A–L rationale follows — deliberately chosen (and reordered once already, from an earlier Name-first layout) to line up with how a typical distributor price sheet is laid out (Category/Type, Part Number, Vendor, Description, then price columns), so a contractor can paste several adjacent columns straight out of a real distributor spreadsheet with minimal remapping, per direct user request/example (a Kichler pricing sheet: Category, Part Number, Vendor, Description, Retail, Net, Sell Price). Two consequences of this that aren't obvious from the code alone: (1) there is no separate "Name" column anymore — `parseFixtureRows` uses Part Number as the imported item's name directly (falling back to Description only in the edge case of a row with a description but no part number, so an import never produces a nameless fixture), so a row is only skipped as empty when *both* are blank, not based on a Name cell. (Name was originally Part Number *+* Description concatenated together, changed to Part Number alone per direct request, since the description reads redundant right next to the name everywhere a fixture displays — both fields are still stored and shown separately, just not concatenated.); (2) `findFixtureHeaderRow` now detects the header row by column A starting with "category" (was "name" when Name was column A) — if column A's role ever changes again, that detection has to move with it. Fixture Type matching is deliberately lenient for this same reason: a distributor sheet's own type-ish column commonly reads like `"Accent : Integrated"`, so `parseFixtureRows` only matches the text before the first `:` (stripped of non-letters) against `FIXTURE_TYPES` — pasting that text in unedited still resolves to `accent`. Distributor is matched by name the same way described above (case-insensitive, unmatched imports unassigned).
  - **"Undo this import"** (`lastFixtureImport`, `undoLastImport()`) exists for the "uploaded the wrong spreadsheet" recovery case — every row `importFixturesFromFile` adds gets a fresh generated id (`'c'+Date.now()+'_'+uidCounter`), so the whole batch's ids are captured in one module-level variable right after a successful import, and undo just filters `state.customFixtures` by that id set and fires a matching `sb.from('custom_fixtures').delete().in('id', ids)` — no snapshot of row contents is kept, so it stays correct even if the user edits one of the freshly-imported rows before undoing (it still removes the row by id). Deliberately tracks only the *most recent* import, not a full stack — a second import (or a page reload) replaces/loses the ability to undo the one before it, same "good enough for the realistic case, not a general system" scope as the bulk-delete/multi-select feature nearby. The button lives inside `#importStatus` itself (a `<span>` for the message text + the button, both children of the status div, which is now `display:flex` to lay them out on one row) rather than as a separate element, so it only ever appears alongside the exact success message it applies to. No confirm-before-navigate/placement-in-use guard beyond the existing `confirm()` dialog — same accepted risk as the existing single-fixture "Delete" button already has (CLAUDE.md note above), not something this feature needed to newly solve.
- **Removing a built-in fixture from the library doesn't delete anything — it can't, `BASE_FIXTURES` is a hardcoded array, not database rows.** It sets `hidden:true` on that id's entry in `state.fixtureOverrides` (the exact same table/mechanism already used for editing a built-in's fields — a "removed" fixture is just an override with `hidden:true`, possibly alongside other customized fields). `allFixtures()` filters these out; `findFixture(id)` deliberately does *not* apply that filter, so a fixture already placed on an existing design keeps resolving correctly (via `BASE_FIXTURES.find` + override) instead of silently breaking that area's rendering/pricing once hidden. Restorable from "Removed from your library" at the bottom of each category tab (`renderFixtureMgmtList`) — `restoreFixture()` deletes just the `hidden` key, keeping any other customization on that override, and only deletes the whole override row if `hidden` was the only thing in it. Custom fixtures (`state.customFixtures`) are unaffected by any of this — those are real rows, and their "Delete" button (label switches based on `f.custom`) still does a real, permanent `custom_fixtures` delete with no restore path, exactly as before.
- **PDF / export** (~line 2154): "PDF export" is actually the browser's native `window.print()` against a print stylesheet (`applyQuoteAppearance`, `applySectionOrder` reorder printed sections per company settings). A separate path, `exportAreaImagesOnly`/`captureAreaDesignImage` (~line 2354), renders a design (photo + beams + wires + strips) onto an offscreen `<canvas>` at higher resolution to produce a flat JPG when the user wants images without the quote table.
  - **The printed/PDF area photo is deliberately *not* the same fixed-height `object-fit:cover` thumbnail used on-screen in the projects list.** On screen, `.area-card-photo` is a short fixed-height crop (150–260px depending on breakpoint) — fine for a scrolling list of small cards, but it crops off whatever doesn't fit, which can (and did — a real user-reported bug) hide part of a design, like a beam or fixture placed lower in the photo. The `@media print` block overrides this to `height:auto` + `object-fit:contain` so the printed quote — the actual customer-facing deliverable — always shows the complete, uncropped photo at its real aspect ratio. If you ever need to change how tall the print photo renders, override `height`/`object-fit` in `@media print`, not the base `.area-card-photo` rule — changing the base rule affects the on-screen thumbnail too.
    - **`height:auto` + uncropped, taken alone, created a second real bug: a strongly portrait-oriented photo could render tall enough to nearly fill an entire printed page by itself** (a real exported PDF, inspected directly — a customer quote with several areas came out to 5 pages, each showing one area card followed by a large blank gap, because `.area-card{break-inside:avoid}` (see the historical note on that class two entries up — actually see the print-CSS block directly) means a card that doesn't fit in whatever's left on the current page gets pushed to a fresh one *whole*, and a near-full-page-tall card leaves almost nothing on the page it left behind). **`PRINT_PHOTO_CAP_H`** (near `renderAreaList`, **now 4in — cut down from 5.5in, see the "too much blank space" fix note further down for why**) is the fix: each area card gets an inline `--print-photo-cap-w` CSS custom property computed from that area's own `photoAspect` (`PRINT_PHOTO_CAP_H * aspect`), consumed only inside `@media print` as a `max-width` on `.area-card-photo` (plus `margin:0 auto` to center it). Combined with the existing `height:auto`, this caps the photo's rendered *height* to `PRINT_PHOTO_CAP_H` regardless of aspect ratio — a max-width proportional to the aspect, on an `object-fit:contain` image with auto height, works out to a max-height — and never changes the container's aspect ratio, so every existing objectBoundingBox-fraction overlay (`buildLitAreaPath`/`buildLitFadeMaskImage`) stays correctly aligned with zero changes needed there. A photo whose natural rendered height is already under the cap is unaffected (the `max-width` never binds). Verified against a real Chromium print (`chrome --headless --print-to-pdf`, since `window.print()`'s own dialog can't be automated) with a portrait and a landscape test photo side by side — only the portrait one visibly narrowed at the old 5.5in value.
    - **The price breakdown (subtotal/discount/tax/total, `#projQuoteTfoot`) could also land alone on its own near-empty page, separated from the item rows it's summing** — a second real bug caught in that same exported PDF (the last item row at the bottom of one page, all three subtotal/tax/total rows alone at the top of the next). `#projQuoteTfoot{break-inside:avoid}` (see the thead/tfoot note above) only stops the footer's own rows from splitting apart from *each other*; it does nothing to stop the whole block being pushed onto a fresh page away from the table. First fix was `break-before:avoid` on that same rule (glue the footer to whatever row it lands near) — the renderer won't place a page break immediately before the footer, so it either stays attached to the item rows or pulls the last of them down with it. **That wasn't enough on its own**: the user's follow-up ("still split", with a screenshot) showed it still let the table visibly split mid-list — 4 of 5 items on one page, the 5th item + footer alone on the next; technically not "footer stranded alone" anymore, but still reads as a split table to a customer. The fix tried at the time was `[data-section="quote"]{break-inside:avoid;}` on the whole "Project quote" section (heading + table + footer) — push the entire section onto a fresh page as one unit whenever it's short enough to plausibly fit on one page.
    - **That section-level rule has since been removed, along with `#projQuoteThead{break-after:avoid;}`, and `PRINT_PHOTO_CAP_H` cut from 5.5in to 4in — a real user report ("too much blank space on each page under the design," screenshot of one area + a short quote nowhere on that page) traced to this exact mechanism backfiring far worse than the bug it fixed.** `#screen-project-detail` is a flex column (`display:flex;flex-direction:column`, needed for the reorderable `data-section` blocks via `applySectionOrder()`'s inline `order`), and Chromium's fragmentation of `break-inside:avoid` on a flex item is conservative enough that it doesn't reliably use remaining space that's genuinely there — confirmed directly (real headless-Chromium `Page.printToPDF`, PyMuPDF block positions, plus `Emulation.setEmulatedMedia({media:'print'})` to inspect live layout geometry) with a single-fixture quote whose "Project quote" heading+table+footer only needed ~105pt: it still got pushed to a fresh page 2 even though ~162pt of clear room was left at the bottom of page 1, leaving both pages mostly blank. Removing the section-level rule alone wasn't enough either, for a subtler reason: `.quote-table tr{break-inside:avoid}` + `#projQuoteThead{break-after:avoid}` + `#projQuoteTfoot{break-inside:avoid;break-before:avoid}` chain together and make a *short* table atomic anyway (there's nowhere in a 1-2-row table for a break to land that all three rules don't jointly forbid), reproducing the identical bug from a different rule combination. Removing `#projQuoteThead`'s `break-after:avoid` let the heading and column-header row land on page 1 in the same repro, but the still-glued data-row+footer chunk (needing well under half the remaining space, by direct measurement) *still* jumped to its own mostly-blank page 2 — Chromium's table row-group (`<tbody>`) fragmentation is coarser than per-`<tr>` in practice, and this app already uses three separate `<tbody>`s (`#projQuoteThead`/`#projQuoteTbody`/`#projQuoteTfoot`) for the unrelated thead/tfoot-repeat-per-page fix documented above. Rather than keep fighting table/flex fragmentation internals, the fix that actually closed the gap was cutting `PRINT_PHOTO_CAP_H` from 5.5in to 4in, so there's reliably enough headroom left after a typical 1-2-area project's photo(s) for a modest quote to fit on the same page without hitting this edge at all — verified on the same single-area repro (now genuinely 1 page, footer text ending well before the page bottom) and on a 2-area repro (a second area that doesn't fit correctly starts a fresh page on its own, exactly as it should, and the quote then sits right under it with no forced gap). **This doesn't (and can't) eliminate every page break** — a project with enough areas/line items to genuinely exceed one page still paginates normally, which is correct, expected behavior, not a bug; only the common "clearly would fit but got bumped anyway" case is what this fixes. If a future report shows blank space again, check whether it's a *pointlessly* wasted gap (pull `PRINT_PHOTO_CAP_H` down further) vs. content that legitimately doesn't fit (not a bug) — and don't reintroduce `[data-section="quote"]{break-inside:avoid}` or `#projQuoteThead{break-after:avoid}` without re-solving the transitive-glue interaction above.
  - **The static/print beam overlay (`buildStaticOverlaysHtml`) sizes its beam as a CSS percentage of width (`aspect-ratio:1` + `width:N%`, via `buildBeamHtml`'s `responsive=true` path) instead of the fixed pixel `width`/`height` the live editor uses (`renderOverlays`, `responsive` omitted/false) — this is a deliberate, load-bearing difference, not an inconsistency to "clean up."** A real user-reported bug: a beam that reached well up into a tree's canopy in the live editor showed only a short way up it in the exported PDF/print. Two fixes were tried before landing on this one — both worth knowing about so they aren't retried: (1) deriving `rect.height` from `area.photoAspect` instead of the on-screen cropped box's real height — didn't help, because `clampR`'s formula barely depends on `rect.height` at all in practice (only as a rarely-binding upper-clamp ceiling); the real dependency was on `rect.width`. (2) Re-running the pixel-based generation on the `beforeprint` event (the browser guarantees print media styles are already applied by the time it fires, unlike measuring synchronously before `window.print()`) — this *did* work in a synthetic test (resizing the viewport and dispatching a fake `beforeprint`), but the user reported the PDF was still wrong, meaning real print/PDF pagination doesn't reliably reflect back to `getBoundingClientRect()` the way an on-screen resize does — there's no JS hook that reliably observes the print layout's actual width. **The fix that actually worked sidesteps measuring entirely**: the beam sizes itself as `width:${R*2/rect.width*100}%` (width cancels out of the formula algebraically, so the percentage is independent of whatever `rect.width` was at generation time) with `aspect-ratio:1;height:auto` to stay square regardless of the container's own aspect ratio, using a fixed reference rect (`{width:380, height:380/area.photoAspect}`) purely to reproduce the existing reach-scaling and min/max-clamp formula — its absolute size doesn't matter since the percentage divides it back out. The browser's own layout engine then keeps it correctly sized at *any* future width — on-screen, print, resized, anything — with no JS involved at all, which is what makes it actually reliable where re-measuring wasn't. This needed a supporting CSS change too: `.area-card-photo .overlays .placement{width:100%;height:100%;}` — percentage sizing on the beam only resolves against a containing block (`.placement`) that itself has a *definite* size, and `.placement` is normally unsized (auto/shrink-to-fit), which the live editor's pixel-based children don't care about but a percentage-based child does; this rule is scoped to `.area-card-photo` specifically so it can't reach the live editor's own `#overlays .placement`, which must keep pixel-based sizing for its drag-handle math (`hx`/`hy`/`wx`/`wy`, used by `renderOverlays`/`updatePlacementBeam`, both of which correctly omit `responsive`). Verified by resizing the viewport with *no* JS re-render call at all and confirming the beam's computed pixel size tracked the container width proportionally (and stayed exactly square) purely via CSS. If you ever add another static (non-live-editor) rendering of a beam, give it this same percentage-based treatment from the start — a fixed-pixel size frozen at generation time is the mistake that started all this, and re-measuring-at-the-right-moment is a trap that looks like a fix but isn't reliable for print specifically.
  - **`html`, `body`, and `.app` all carry their own explicit background color, and print needs to override all three — not just one or two of them.** `html,body{background:var(--ink-2)}` is declared *twice* (the base rule, and again inside `@media (min-width:860px)`), and `.app{background:var(--ink)}` separately. In light theme both `--ink` (`#F4F1EA`) and `--ink-2` (`#E7E2D6`) are cream/tan tones, not white. **First attempt at this fix only overrode `body` and `.app`, and missed `html`** — since `html` is the outermost element, its leftover tan background still bled through as a "weird tan" block below the quote content, which is exactly what the user saw even after that first fix shipped; verified by reproducing it (checking computed `background-color` on all three elements with only `body`/`.app` overridden showed `html` still resolving to `--ink-2`'s tan, `rgb(231,226,214)`) before fixing it for real with `html,body{background:var(--pdf-bg-color,#fff);}` plus `.app`'s own override. If you ever touch this again: grep for every `background:var(--ink` and `background:var(--ink-2` declaration first, not just the ones that seem obviously relevant — there were three, in three different places, and it was easy to fix two and still leave the bug in place.
  - **Quote footer**: `state.company.footerText` (free text, `companies.footer_text` column) renders into `#printFooter` via `renderCompanyFooterForPrint()` (called from `renderCompanyPreview()`, same pattern as the header). It's deliberately *not* part of the reorderable `sectionOrder` list — a footer is always last by definition — so it isn't marked with a `data-section` attribute; instead `#printFooter` has a hardcoded `order:100` (all reorderable sections are 0–4), guaranteeing it's always last regardless of section order. Line breaks in the textarea are preserved via a `\n` → `<br>` replace after `escapeHtml()`.

### Auth & backend (Supabase)

- **Client init** (~line 894): `sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY)`, loaded via the `@supabase/supabase-js@2` CDN script tag in `<head>`. The anon key is intentionally public — it's safe to be in client-side code because row-level security (RLS) is what actually restricts access, not the key itself. Never put the `service_role` key in this file.
- **Schema**: see `supabase/schema.sql` — the actual source of truth for the live schema (`companies`, `profiles`, `projects`, `areas`, `custom_fixtures`, `fixture_overrides`, all RLS policies, the `handle_new_user` trigger), kept manually in sync since there's no migrations CLI wired up. If you change the schema, update `supabase/schema.sql` in the same session (and run the SQL yourself via the Supabase SQL Editor — there's no tool/CLI access to the database from this environment).
- RLS is scoped through a `security definer` helper function `current_company_id()`. **Do not** write a `profiles` RLS policy as a raw subquery against `profiles` itself (e.g. `company_id in (select company_id from profiles where id = auth.uid())`); that causes Postgres error 42P17 "infinite recursion detected in policy," which is exactly what happened the first time this was built. Always route self-referencing lookups through the `security definer` function instead.
- **Signup auto-creates a tenant**: a Postgres trigger (`handle_new_user`, defined in Supabase) fires on every `auth.users` insert, reading `company_name`/`full_name`/`access_code` out of the signup call's `options.data` and creating one new `companies` row + one `profiles` row (`role: 'owner'`) per signup — but only if `access_code` matches the hardcoded gate value (see the access-code note above); otherwise it raises and the whole signup fails. There is currently no invite flow for adding a second person to an existing company — every signup is a brand-new tenant.
- **App-side auth flow** (~lines 2690–2740): `sb.auth.onAuthStateChange` (subscribed once in `init()`) is the single source of truth for entering/leaving the app — `enterAppForUser()` on a session, `leaveApp()` on none. Session persistence (surviving a page reload) is handled entirely by supabase-js itself via its own `localStorage` key; nothing in this app's code re-implements that. The `#accountModal` element is dual-purpose: it's the mandatory sign-in/sign-up gate when logged out (`.app` is hidden via the `auth-hidden` CSS class until a session exists — see `setAppVisible`), and becomes an account-info/log-out panel when logged in — `openAccountModal()` branches on whether `authUser` is set.

**Distributor accounts** (schema-only so far, not yet run against the live database — see the "Distributor accounts" migration item below before relying on any of this in production): a `companies.account_type` column (`'contractor'` default, or `'distributor'`) turns a company into a catalog-provisioning org instead of a lighting-design one. Built to require as little new code as possible — most of it is reuse of things that already existed:
- **A distributor's catalog is just their normal `custom_fixtures` — no new fixture-management UI exists for it.** Their own "Sell price" *is* the list price a linked contractor sees; their "Cost" stays private (see the relabeled `#fxCostLabel`/`#fxPriceLabel`, toggled in `applyAccountTypeUI()`).
- **`distributor_links`** (`distributor_company_id`, `contractor_company_id`, `multiplier`) is the relationship + pricing-tier table — one row per contractor a distributor has onboarded. **`distributor_invites`** (`code`, `distributor_company_id`, `multiplier`) is how a not-yet-existing contractor gets linked: `handle_new_user()` now checks the signup form's *existing* `access_code` field against `distributor_invites` first, before falling back to the hardcoded distributor-provisioning code (`DUSKLINEDISTRIBUTORV1`) and then the ordinary beta code — no new signup UI field was needed, the one access-code input just has three possible meanings now, disambiguated server-side.
- **`contractor_catalog()`** is the actual live-pricing mechanism: a `security definer` function joining a linked distributor's `custom_fixtures` to `distributor_links`, computing `cost = price * multiplier` and stripping the distributor's own `cost`/`vendorId` before a contractor ever sees a row. It runs with elevated privileges (bypassing the distributor's own `custom_fixtures` RLS internally) but filters on `current_company_id()` in its `where` clause, so a contractor genuinely cannot read another company's raw fixture rows or list price — only this computed, per-caller-filtered result. (Originally a view — see the Security Advisor note below for why it's a function now.) Loaded into `state.distributorCatalog` (`loadCompanyDataFromDb`) and merged into `allFixtures()` alongside `BASE_FIXTURES`/`state.customFixtures`, running the same `fixtureOverrides`-merge + `hidden`-filter logic already used for built-ins — a contractor "editing" a distributor fixture is really just writing a `fixture_overrides` row for it, keyed the same way, with only `price` ever allowed through (enforced in `saveFixtureForm`'s `fixtureFormState.locked` branch, not just in the UI's `disabled` attributes).
- **Do not confuse this with the pre-existing per-company `distributors` table** (`state.distributors`, `renderDistributorList`) — that's an unrelated, much older feature: a simple vendor-contact address book (name/email/phone/account#) used only for PO grouping via a fixture's own `vendorId`. The naming collision is real and was a known risk going in; the distributor-*account* feature deliberately uses different table/function names (`distributor_links`, `distributor_invites`, `distributorSourced`, `distributorCatalog`) and its own section banner (`// ---------------- distributor ORG: contractors screen`) to stay distinguishable in code, and its Company-tab UI is labeled "Connected distributor catalogs," never bare "Distributors," to stay distinguishable on screen too.
- **Column-level grants, not just RLS, protect `account_type`**: the existing "Users can update their own company" RLS policy has no column restriction, so without this a contractor could `PATCH` their own `companies` row and self-promote to `account_type:'distributor'` via the REST API directly. `supabase/schema.sql` now does `revoke update on companies from authenticated` followed by an explicit column-list `grant update(...)` that excludes `account_type` — if you ever add a new company-level setting, it needs adding to that grant's column list too, or saving it will silently fail (RLS passes, the column grant blocks it).
- A distributor account gets a different tab set entirely (`applyAccountTypeUI()`, called from `afterProfileLoaded()`): no Projects tab (no lighting-design concept for a distributor org), a new "Contractors" tab (`#screen-distributor-contractors` — generate invite codes, edit each linked contractor's multiplier) in its place, and lands on Fixtures instead of Projects after login.
- **`distributor_contractor_list()`/`contractor_distributor_list()`** exist purely to work around a real bug/gap: the app originally tried to show "the other side's" company name via a plain PostgREST embedded join (`companies!*_company_id(name)`), but `companies` SELECT RLS only ever allows seeing your *own* company row, so that join silently came back null for every linked company regardless of the `distributor_links` relationship (visible as "Unnamed contractor" and a blank distributor name). Same fix pattern as `contractor_catalog()`: a narrow, per-caller-filtered result exposing only what's needed (a contractor's name/phone/email/address to their distributor; just a distributor's name to a contractor) instead of opening up `companies` RLS more broadly. `distributor_links.label` is a separate thing — a distributor's own freeform nickname for a contractor (seeded from the invite's `label`, editable after), distinct from the contractor's real business name.
- **All three of the above (`contractor_catalog()`, `distributor_contractor_list()`, `contractor_distributor_list()`) started life as plain Postgres views, not functions.** Supabase's Security Advisor flags any view lacking `security_invoker` as a CRITICAL "Security Definer View" finding — correctly, in the general case, since a view like that runs with its *owner's* privileges rather than the calling user's, silently. Each view's own `where ... = current_company_id()` clause already scoped its result to only what the caller was entitled to see, so there was never an actual data leak here, but there's no way for the linter to know that from the view definition alone. Converted to explicit `security definer` functions instead (same shape/filtering, `sb.rpc(...)` from the client instead of `sb.from(view).select('*')`) — a function has to *declare* `security definer` to get elevated privileges, which is the audited, intentional version of the exact same behavior a view gets implicitly, and matches the pattern `current_company_id()`/`is_platform_admin()`/`admin_company_summary()` already used (none of which the Advisor flags). If a new "expose a narrow cross-company result" need ever comes up again, write it as a `security definer` function from the start rather than a view — it does the same job without tripping this advisory.
- **`handle_new_user()` hides every `BASE_FIXTURES` starter/example fixture for every brand-new signup** (both account types), via the same `fixture_overrides{hidden:true}` mechanism a user gets from manually clicking "Remove from library." A fresh account should only ever see fixtures it added itself or (for a contractor) a connected distributor's real catalog, not this app's generic example catalog. **The id list is hardcoded in the trigger and has no shared source of truth with the `BASE_FIXTURES` array in `index.html`** — if you ever add/remove/rename a built-in fixture id, update both places or new signups will either still see a stale example fixture or silently try to hide an id that no longer exists (harmless, just a no-op row). This only runs at signup — it does not retroactively touch any already-existing company's fixture library.
  - **Unlike a manually-hidden fixture, these no longer show up in "Removed from your library" with a Restore button.** Since every one of `BASE_FIXTURES`' ~17 entries is hidden by this trigger for every single signup, that list would otherwise ALWAYS show the complete example catalog on a brand-new account — real user-reported clutter (a screenshot of ten placeholder fixtures like "Contour Spot — Small" all offering "Restore"), and there's no real scenario where a contractor wants one of these back in their actual business catalog. `renderFixtureMgmtList()`'s `removed` list (`index.html`) now only pulls from `state.distributorCatalog`, not `BASE_FIXTURES` — a hidden *distributor-sourced* fixture (a real catalog item from a connected distributor) still shows up there with Restore, since a contractor might legitimately want that back. This is purely a display change; the underlying `fixture_overrides{hidden:true}` rows are untouched, so if a `BASE_FIXTURES` id genuinely needs restoring for some account it's still just a manual Storage/SQL Editor row delete away.
- **A distributor can remove a linked contractor** (`renderContractorLinkList()`'s "Remove" button) — a plain `delete` on that `distributor_links` row, already covered by the existing "Distributor manages own links" RLS policy. No cleanup needed on the contractor's side: `contractor_catalog()` is a live join through `distributor_links`, so once the row is gone the distributor's fixtures just stop appearing on the contractor's next load, the same live-update mechanism that already handles a multiplier/price change.
- **A distributor's invite codes can be deleted** (`renderInviteList()`'s "Delete" button, next to "Copy" for still-pending codes) — a plain `delete` on that `distributor_invites` row, already covered by the existing "Distributor manages own invites" RLS policy (`for all`, so delete was already allowed — no new SQL needed for this one). Deleting a **pending** code just revokes it (nobody can redeem it anymore). Deleting an **already-redeemed** code only removes that history row — it does not touch the `distributor_links` row it created, so the contractor it was used by keeps their access; the confirm-dialog wording says this explicitly so it isn't mistaken for a "remove this contractor" action (that's the separate "Remove" button above).
- **A newly-onboarded contractor automatically gets their distributor added as a vendor contact**, so PO grouping works for distributor-sourced fixtures without the contractor doing anything manually. Before this, `contractor_catalog()` deliberately strips `vendorId` from a distributor's raw `custom_fixtures` row (a distributor's own internal vendor id has no meaning to the contractor), so every distributor-sourced fixture had `vendorId: null` — no vendor to send a PO to, ever, unless the contractor happened to manually create a matching `distributors` (vendor-contact) row themselves. Fixed at the source: `handle_new_user()`'s invite-redemption branch now also inserts a `distributors` row for the new contractor, pre-filled from the distributor's own `companies` profile (name/email/phone) and tagged with the new `distributors.source_distributor_company_id` column (nullable, only ever set on these auto-created rows — a manually-added distributor contact leaves it null). `allFixtures()` then matches each distributor-sourced item's `distributorCompanyId` (now captured explicitly in `loadCompanyDataFromDb`'s `state.distributorCatalog` mapping, since it's a top-level `contractor_catalog()` column, not part of the fixture's own `data`) against `state.distributors` by that column, and sets `vendorId` accordingly before the `fixture_overrides` merge — safe to do unconditionally since an override can never set `vendorId` for these (`saveFixtureForm`'s distributor-sourced lock only ever allows `price` through). **Only applies going forward, from the invite-redemption moment on** — an already-linked contractor from before this shipped is not backfilled, and still has to add the distributor as a vendor contact by hand if they want PO grouping for those fixtures. If a contractor deletes the auto-created contact, distributor-sourced fixtures just fall back to `vendorId: null` again (same as pre-this-feature behavior), no crash.
- **The "Distributors" vendor-contact address book is hidden entirely on the Company tab for distributor accounts** (`applyAccountTypeUI()` toggles `#distributorVendorSection`, wrapping the section-head/list/empty-note that used to be three loose siblings) — a distributor company doesn't have suppliers of its own to track POs against the way a contractor does, so the section (and its "+ Add distributor" button) never made sense there. Purely a visibility toggle — the underlying `distributors` table/RLS/`state.distributors` are completely unaffected, and a distributor account that somehow already had rows there (unlikely, but not impossible via the REST API directly) keeps them, just doesn't see the UI for managing them.

**Branches** (`distributor_branches` table, "Branches" section on the Contractors screen): lets a distributor with multiple physical locations give each one its **own catalog** (`custom_fixtures.branch_id`) and its own contact identity (name/email/phone/address), and assign a contractor to whichever branch is actually closest to them — both for which fixtures/pricing they see, and for which branch's contact info a PO from them lands on. Requested specifically to fix POs going to a branch 100km away instead of the closest one. `null` means "the main/unbranched catalog or link" everywhere this appears (`custom_fixtures.branch_id`, `distributor_links.branch_id`, `distributor_invites.branch_id`) — a distributor that never creates a branch sees zero change, since every row they have just keeps `branch_id = null`, exactly its pre-this-feature meaning.
- **Branch isolation happens in `contractor_catalog()`**: `cf.branch_id is not distinct from dl.branch_id` (the null-safe equality Postgres needs here — a plain `=` would silently drop every unbranched null-vs-null match) means a contractor only ever sees the exact branch's fixtures their own `distributor_links` row is assigned to, never another branch's and never the main catalog if they're assigned to a branch (or vice versa).
- **The Fixtures tab gets a branch switcher** (`#branchTabs`, `renderBranchTabs()`) — "Main catalog" plus one button per branch — but only for a distributor account that's actually created a branch; hidden entirely otherwise (checked via `authProfile.accountType==='distributor' && state.branches.length`), so this is invisible for every contractor account and for a distributor with none. `state.currentBranchId` (null by default) tracks which one is selected, both for filtering `renderFixtureMgmtList()`'s display (`(f.branchId||null) === state.currentBranchId`, normalizing a pre-branches fixture's `undefined` down to null so it correctly shows under "Main catalog") and for stamping new fixtures with the right `branch_id` on create (`saveFixtureForm`) and import (`importFixturesFromFile`).
- **Assigning a contractor to a branch happens two ways**: at invite-generation time (`#inviteBranchField`/`#inviteBranch`, only shown once a branch exists, captured into `distributor_invites.branch_id` and carried into the resulting `distributor_links.branch_id` by `handle_new_user()`), or afterward by editing an already-linked contractor's row directly (`renderContractorLinkList()`'s per-row branch `<select>`, same pattern as the existing multiplier/label editing, `sb.from('distributor_links').update({branch_id})`).
- **The auto-vendor-contact insert (see the note above) is now branch-aware**: `handle_new_user()` prefers the assigned branch's own name/email/phone over the parent distributor company's (`coalesce(b.name, c.name)` etc., via a `left join distributor_branches` on the invite's `branch_id`), falling back to the company's info when no branch was picked — so a contractor's auto-created vendor contact, and therefore where their POs go (see below), is whichever address is actually closest to them. **This needed a second `handle_new_user()` redefinition** — the branches migration's first pass added `distributor_invites.branch_id` and the branch-aware `contractor_catalog()`/list functions, but never actually updated the trigger to read that new column, so an invite generated with a branch selected would have silently created a link with `branch_id` still null. Caught (by re-reading the diff before handing off the SQL) and fixed before ever running live — if branch assignment ever seems to silently not take effect again, `handle_new_user()` not referencing `v_invite.branch_id` is the first thing to check.
- **`distributor_contractor_list()`/`contractor_distributor_list()` both got trailing `branch_id`/`branch_name` columns** (append-only — `CREATE OR REPLACE FUNCTION` can extend a `RETURNS TABLE` function's output list at the end, but can't reorder or remove existing columns, which is why these were added last rather than interleaved) so both sides of a distributor/contractor relationship can see which branch it's on — the distributor's own "Linked contractors" list, and the contractor's "Connected distributor catalogs" display on the Company tab.

**Purchase-order emails now include a real PDF, not just plain text** (`buildPoPdfBlob()`/`uploadPoPdf()` in `sendPoEmail()`, near `buildPoDocumentHtml()`): "Email PO" was, and still is, a `mailto:` link — and `mailto:` **cannot carry file attachments**, a hard email-client limitation no code change can get around. So instead, `buildPoPdfBlob()` renders the same PO content as a real PDF via jsPDF + its autoTable plugin (pinned CDN scripts in `<head>`, `jspdf.umd.min.js` + `jspdf.plugin.autotable.min.js`, same SRI-pinning convention as every other CDN script here), `uploadPoPdf()` uploads it to the existing public `photos` Storage bucket under a new `{companyId}/pos/` prefix (no new bucket or policy needed — that bucket's RLS keys off the leading company-id path segment, not the file type or subfolder), and the resulting public URL gets appended to the email body as a "PDF copy: ..." line. A failed PDF upload (`uploadPoPdf` catches internally and returns `null`) doesn't block the email — `sendPoEmail()` just falls back to the plain-text-only body, its exact pre-this-feature behavior. Since generating + uploading is a real network round trip (unlike the old, instant, purely-local mailto: build), the "Email PO" button now shows a "Preparing…" disabled state for that moment (`renderPoSection()`'s click handler) so a click doesn't look like it silently did nothing.
- **A real backend email service (e.g. Resend) with a true native attachment was considered and explicitly not built** — it would need new server-side code (this app has none currently — no Edge Functions, nothing beyond client + Supabase table/RLS/DB functions) and a new third-party account/API key, similar in kind to the Google Maps key setup. The link-to-a-hosted-PDF approach above was chosen instead: no new infrastructure, no new cost, keeps the existing "opens the user's own mail app" mailto: flow intact. Revisit only if real demand for a true attachment (vs. a link) shows up.

**Distributors can see contractor usage right on the Contractors screen** (`distributor_contractor_list()` extended with `joined_at`/`last_sign_in_at`/`project_count`, rendered as a small `.fx-spec` stats line under each contractor's name in `renderContractorLinkList()`) — signup date, last login (or "Never logged in"), and how many projects they've created, without Isaac needing to be involved. Same `security definer` cross-company read as `admin_company_summary()`/`admin_list_feedback()` (needs `auth.users`, which isn't otherwise exposed to the client), but gated differently: not `is_platform_admin()`, since a distributor is allowed to see usage for their *own* linked contractors — the `where dl.distributor_company_id = current_company_id()` clause already scopes it correctly, the same way every other distributor-facing function here works. **This also needed drop-then-create**, not `create or replace` — same 42P13 lesson as the branches migration, since this adds trailing columns to an existing `RETURNS TABLE` function.

**Feedback** (`feedback` table, 💬 header icon on every screen next to the theme toggle, `#feedbackModal`): a simple "send Isaac feedback" button, writing to a table instead of a `mailto:` link — lands as a reviewable list (`admin_list_feedback()`, a new "Feedback" section at the bottom of the Admin dashboard) rather than depending on the user having a mail client configured, and doesn't expose Isaac's own email address in the page source the way a `mailto:` link would. RLS is insert-only for regular users (`for insert with check (company_id = current_company_id() and user_id = auth.uid())`) — nobody needs to read back feedback from inside the app, only submit it; `admin_list_feedback()` is the only read path, gated on `is_platform_admin()` same as `admin_company_summary()`.

**Fixture import's "Name" is now Part Number alone, not Part Number + Description concatenated** (`parseFixtureRows`, falls back to Description only when Part Number is blank) — changed per direct request since the description already shows separately everywhere a fixture displays, so the concatenated version read as redundant/cluttered. Column layout (A–L, Part Number still column C) is unchanged; only what gets written into the `name` field changed.

**Platform admin dashboard** (`#screen-admin`, `renderAdminDashboard()`): lets Isaac (not a distributor or contractor — the app owner) see every signed-up company grouped by distributor, with signup date, last login, and project/area counts, to gauge real usage/value of the distributor network. `profiles.is_platform_admin` is a boolean settable **only by hand via the SQL Editor** — there is no UI to grant it, deliberately, the same way the very first distributor account had to be provisioned manually. It's **orthogonal to `account_type`**: an admin's own account is still a normal contractor or distributor underneath (`applyAccountTypeUI()` toggles the `#tabAdmin` tab independently of the Projects/Contractors tab logic). All data comes from one function, `admin_company_summary()` — `security definer`, so it can read `auth.users.last_sign_in_at` (which Supabase Auth already tracks natively and isn't otherwise exposed to the client) and project/area counts across every company despite normal per-company RLS, but internally gated on `is_platform_admin()` so a non-admin calling it via `sb.rpc` just gets zero rows back, never an error or another company's data. Deliberately scoped to identity + usage only (name/type/signup date/owner email/last login/project & area counts), not phone/tax/branding — same "expose only what's needed" principle as the `distributor_contractor_list()`/`contractor_distributor_list()` fix above. **`profiles` also had no column-level UPDATE protection at all** (the same class of gap `companies.account_type` had) — closed with a flat `revoke update on profiles from authenticated`, safe because the client never actually updates `profiles` from anywhere (confirmed: only ever reads it, in `refreshAuthProfileRow`).

**Password reset**: `onAuthStateChange` (`index.html`, near the end of `init()`) now branches on `event==='PASSWORD_RECOVERY'` *before* its normal session-presence check — without this, a user landing via a reset-password email link would fall straight through into the app with their old password still active, never prompted to set a new one. Reuses the existing `#accountModal`/`authMode` infrastructure rather than a separate modal: `authMode` gained a third value, `'recovery'`, which `setAuthMode()` handles by hiding the mode tabs/company/name/access-code/email fields and relabeling the (repurposed, not a new element) password field. `submitAuthForm()` branches on `authMode==='recovery'` to call `sb.auth.updateUser({password})` instead of sign-in/sign-up, then explicitly calls `enterAppForUser()` afterward. **The `PASSWORD_RECOVERY` branch bypasses `openAccountModal()`** (which normally sets the modal's button visibility), so it explicitly resets `#accountSubmit`/`#accountLogout`/`#accountCancel`/`#authForms`/`#accountInfo` visibility itself — otherwise a stale reset link opened while already signed in elsewhere could inherit the modal's last real state (e.g. the submit button still hidden from "Account" mode). Needs `https://duskline.ca` in Supabase's Auth → URL Configuration allowed-redirects list for the reset link to land correctly — not verified from this environment, check the dashboard directly.

**Isaac's own admin account can act as a catalog/invite-code source without becoming a distributor account_type**: he wanted "the distributor master-account stuff" (invite codes, a catalog other people's accounts see, but they can still add their own fixtures) available directly from his existing admin login, without losing his Projects tab or otherwise turning his account into a distributor. This worked out to almost no new code, because it turned out **only one thing actually required `account_type='distributor'`**: the join inside `contractor_catalog()`. Everything else a distributor account uses — `distributor_links`/`distributor_invites`/`distributor_branches`, the Contractors-screen queries, `handle_new_user()`'s invite-redemption branch (including the auto-vendor-contact insert) — is already keyed purely on `current_company_id()`/a company id, never on `account_type`, confirmed by reading `loadCompanyDataFromDb`'s own comment on why it runs all three distributor-adjacent RPCs unconditionally for every account. So the fix was two small, narrowly-targeted changes rather than a new feature:
- `contractor_catalog()`'s `join public.companies c on c.id = cf.company_id and c.account_type = 'distributor'` relaxed to `... and (c.account_type = 'distributor' or exists (select 1 from public.profiles p2 where p2.company_id = c.id and p2.is_platform_admin))` — a company owned by a platform admin can now be a catalog source too, no new column needed since `is_platform_admin` already exists and is already admin-only-settable.
- `applyAccountTypeUI()` (`index.html`): tab visibility for the two role-tagged tabs (`data-role="contractor"` Projects, `data-role="distributor"` Contractors) now shows **both** when `authProfile.isPlatformAdmin && !isDistributor`, instead of the normal either/or. A genuine distributor account_type is completely unaffected (still Contractors-only, no Projects); a genuine plain contractor is unaffected too (still Projects-only, no Contractors). Login still lands an admin-but-not-distributor account on Projects, same as before — only a real `account_type==='distributor'` lands on Fixtures.
- Branches, the fixture-form Cost/Price relabeling, and the "Distributors" vendor-contact section stay gated on the real `isDistributor` check, unchanged — Isaac's own account still reads as a normal contractor everywhere except the now-visible Contractors tab, so he keeps his own vendor-contact address book and his Fixtures tab keeps its normal Cost/Price labels (which are still accurate: Price there is still exactly what `contractor_catalog()` uses as the invited contractor's list price, same formula as a real distributor). If a second platform admin is ever added, they'd get this too — accepted, since `is_platform_admin` is already a manually-SQL-granted, highly-trusted flag.

**A contractor's "Connected distributor catalogs" card (Company tab) doesn't show the multiplier.** It originally did ("Your cost = their list price × 0.5") — real user-reported concern: a contractor seeing that ratio, plus their own already-known cost for a fixture, can trivially back out the distributor's actual list price, something `contractor_catalog()` already goes out of its way to never send directly (it only ever returns the pre-computed `cost`, stripping the distributor's real `price`/`cost`/`vendorId` — see that function's note above). Showing the multiplier in the UI would leak the same thing by a different route. `renderDistributorLinksSection()` (`index.html`) now renders just the distributor's (and branch's, if any) name, nothing pricing-related.

**Data sync** (~lines 2536–2635, section banner `// ---------------- data sync (Supabase) ----------------`) is where projects/areas/company/fixtures actually move between `state` and Postgres. The load side and the save side are deliberately asymmetric:

- **Loading is eager**: `loadCompanyDataFromDb(companyId)`, called once from `enterAppForUser`, fetches the company row, *all* projects, *all* areas (via RLS alone — no explicit filter needed, since the `areas` RLS policy already scopes it), and the whole fixture library, then populates `state` in one shot — mirroring exactly what the old localStorage blob used to hand back. This is intentionally not optimized (e.g. not lazy-loading areas per-project) — Phase 3 (moving photos out of the JSON) is where that would actually start to matter; doing it now would be solving a Phase 3 problem inside Phase 2.
- **Saving is scoped, not eager.** This is the important part, and it's not just a performance choice — see the migration-progress note above about why a single whole-account blob write isn't safe once more than one person can be editing a company's data at once. `flushCurrentEdits()` only ever writes the company row plus whichever project/area `currentProject()`/`currentArea()` currently point to — never the rest of `state.projects`. It runs on a 5s `setInterval`, on `beforeunload`/`pagehide`, and is explicitly called before anything that would change what "current" means: `closeAreaEditor()`, and `openProjectDetail()` (flushing the *previous* project before switching `state.currentProjectId`). **If you add a new screen or navigation path that changes `currentProjectId`/`currentAreaId`, add a `flushCurrentEdits()` call before that reassignment**, the same way `openProjectDetail` does — otherwise pending edits to whatever was open before can be silently dropped once "current" points elsewhere.
- **Discrete create/delete/explicit-save actions bypass the scoped-save mechanism entirely and write immediately**: new project (`customerSave`), new area (`areaNameCreate`), project delete, area delete, and fixture add/edit/delete/import all do their own direct `sb.from(...).insert/update/delete(...)` call right at the point of the user's click — these don't wait for the tick since they're one-off actions, not continuous edits like dragging a fixture or typing in a field.
- **Row \<-> `state` shape mapping** (`companyToRow`/`rowToCompany`, `projectToRow`/`rowToProject`, `areaToRow`/`rowToArea`) converts between Postgres's snake_case columns and the app's existing camelCase in-memory shape. If you add a field to `state.company`/a project/an area, it needs a matching column in `supabase/schema.sql` *and* both directions of the corresponding mapper function, or it'll silently fail to persist.

**Photo/logo storage** (Phase 3, ~lines 2645–2668): `uploadAreaPhoto(area, dataUrl)` and `uploadCompanyLogo(dataUrl, contentType)` both go through a shared `uploadToPhotosBucket(path, dataUrl, contentType)` — convert the data: URL (produced by the crop canvas) to a `Blob`, upload to the public `photos` bucket at a path starting with `{companyId}/...` (area photos: `{companyId}/areas/{areaId}.jpg`; logo: `{companyId}/logo.{ext}`), and return a cache-busted public URL (`?v=<timestamp>` appended — needed because the path is stable and uploads use `upsert:true`, so without a cache-buster a re-cropped photo could keep showing the old cached image at the same URL). `area.photo`/`state.company.logo` get set to that returned URL, same as they'd previously have been set to a raw data: URL — nothing downstream (rendering, PDF export) needed to change, since an `<img src>` doesn't care whether it's a data: URL or an https:// URL.
- **Any `<img>` that gets drawn onto a `<canvas>` and then read back out (`toDataURL`) needs `img.crossOrigin = 'anonymous'` set before `img.src`, full stop, whenever that image might be a Storage-hosted `https://` URL rather than a `data:` URL.** Two separate places load `area.photo` into a fresh `Image()` for exactly this purpose — `openCropScreen` (Adjust crop) and `captureAreaDesignImage` (the "export design photos as JPG only" flow) — and **both** need the attribute independently; fixing one does not fix the other, since they're unrelated `<img>` elements. `openCropScreen`'s missing `crossOrigin` was caught during Phase 3 testing; `captureAreaDesignImage`'s was a second, separate instance of the identical bug, found later because a user reported "the JPG export button does nothing" (the tainted-canvas exception thrown inside `img.onload` was uncaught, so the `Promise` in `captureAreaDesignImage` just hung forever instead of resolving or rejecting — that's now also caught with a try/catch that resolves `null` on failure instead of hanging). If a third place ever loads a Storage photo into a canvas, it needs this too — it will not fail loudly without it.
- Area/project delete also removes the associated Storage object(s) (`sb.storage.from('photos').remove([...])`, fire-and-forget) so deleted photos don't pile up as orphaned files — see the project/area delete handlers in `renderProjectList`/`renderAreaList`.

**UPDATE — satellite view now lives in CAD plans, not the photo editor** (user request: "move the satellite view function from the picture design to the plans"). The "Satellite view" button (empty-state) and chip (`#btnChangeSatellite`) were removed from the area photo editor; the New-plan modal has a **Satellite view** button (`#planCreateSatellite`, `cadPickSatellite`) next to the file input. `openSatelliteScreen(onDone, opts)` is unchanged apart from `opts`: `{direct:true}` skips the crop screen and calls `onDone(dataUrl, groundWidthFt)` with the whole 1280px square (so the scale stays exact), `{address}` pre-fills the search from the plan's project, `{onCancel}` re-shows the create modal (the satellite screen is z-index 55 below `.edit-sheet`'s 60, so the modal is hidden while it's open). The capture waits in `cadSatPending`; `cadCreatePlan` uses it if no file was chosen (choosing a file clears it), re-encodes it as a JPEG through `cadReadImage`, and sets `pxPerFt = bgW / groundWidthFt` so the plan **starts already scaled** (the editor then opens in Select instead of the Scale tool). The old area-photo path (`finishAreaPhotoCrop` via the crop screen, `area.photoWidthFt`, `pathLengthFt`) is untouched, so existing satellite-sourced area photos keep their scale and wire-length estimates; there's just no longer a way to create new ones. Verified with Google Maps stubbed (localhost isn't in the key's referrer allow-list): cancel/capture/create, saved `px_per_ft` = 6.2554 matching the formula, background rendered, "Scale set — 205' across". The real Google tiles/Static Maps were not exercised from here. Original notes below still describe the shared machinery.

**Satellite view as an area photo** (`openSatelliteScreen`/`useSatelliteView`, ~near `handleFile`) — *no longer reachable from the photo editor, see the update above*: lets a contractor lay out a whole yard from an overhead Google satellite image instead of only a straight-on photo. Deliberately produces *nothing but an image* — `useSatelliteView()` fetches a Google Static Maps API image for whatever view the contractor framed on the interactive picker map, and hands it to the exact same `openCropScreen(sourceUrl, onDone)` every other photo source uses (`finishAreaPhotoCrop`, shared with `handleFile`). From that point on it's indistinguishable from any other area photo — no new area type, no flag, no changes anywhere else in the app (beam rendering, quote math, PDF export all already just treat `area.photo` as "an image").
- **Two Google Maps APIs are in play, for different jobs**: the **Maps JavaScript API** (loaded lazily via `loadGoogleMapsApi()` — only when "Satellite view" is first clicked, not on every page load) drives the *interactive* picker map (`google.maps.Map`, satellite type, plus `google.maps.Geocoder` for the address search) so the contractor can pan/zoom to frame the property visually. The **Maps Static API** is what actually produces the flat, capturable image at "Use this view" time — a plain `fetch()` of a `staticmap?center=...&zoom=...` URL built from that same map's current `getCenter()`/`getZoom()`, so it matches what was just framed on screen.
- **`GOOGLE_MAPS_API_KEY` (near `SUPABASE_URL`) needs to be filled in by hand** — a Google Cloud project with billing enabled, "Maps JavaScript API" + "Maps Static API" + "Geocoding API" all enabled, and the key **HTTP-referrer-restricted** to `https://duskline.ca/*` and `https://www.duskline.ca/*` (same public-but-restricted trust model as `SUPABASE_ANON_KEY`, just enforced by domain instead of RLS) — plus, now that a `dev` branch exists for staging changes before they go live (see the top of this file's workflow notes, or just ask), the `dev` branch's own Vercel preview URL needs adding too or satellite view will silently fail to load there. `openSatelliteScreen()` guards the placeholder-key case with a clear message rather than a cryptic Google error. Not set up or tested from this environment — I can't create a Google Cloud project/billing account myself.
- **The Maps JS bootstrap script is the one deliberate exception to "every CDN script gets a pinned version + SRI hash"** (see the CDN-pinning note below) — Google's loader is generated per-key/per-request and explicitly doesn't support Subresource Integrity. Don't try to "fix" this by adding an `integrity` attribute; it'll just break the load.
- **A satellite-captured photo now carries a real-world scale (`area.photoWidthFt`, `areas.photo_width_ft`), which regular ground photos never have.** Requested specifically so wire runs (and LED strips) drawn on an overhead view can get their length auto-estimated instead of the contractor always typing one in by hand. The math: `useSatelliteView()` computes the FULL capture's real-world ground width in feet from the standard Web Mercator "world pixel" formula (`156543.03392 * cos(lat) / 2^zoom`, meters per pixel of the Static Maps `size` request — note this is independent of the `scale=2` retina param, which only asks for a denser pixel grid over the *same* ground area, not a smaller one) and passes it into `openCropScreen(sourceUrl, onDone, priorGroundWidthFt)` as a new third parameter. Since cropping/zooming in that screen narrows the field of view, `btnCropUse`'s handler re-derives a *smaller* ground width for the final output — `(cropCtx.stageW/cropCtx.scale) * cropCtx.metersPerNaturalPixel * 3.28084` — verified algebraically to reduce to exactly the original capture's ground width when the crop is left at "fit the whole image, no zoom" (`scale===containScale`). "Adjust crop" (re-cropping an already-saved photo later) carries the area's existing `photoWidthFt` back in as `priorGroundWidthFt`, so re-cropping a satellite photo keeps narrowing the value correctly instead of losing it; any non-satellite photo source passes no third argument at all, so `photoWidthFt` stays `null` exactly as before this feature existed. **`finishAreaPhotoCrop` and the "Adjust crop" handler both explicitly set `area.photoWidthFt = groundWidthFt||null` on every save, never conditionally** — this matters because replacing a satellite-sourced photo with an ordinary camera photo must actively clear the old scale, or wire/strip auto-length would keep computing against a ground width that no longer describes this area's actual photo.
  - **`pathLengthFt(path, area)`** (shared by both the wire-run and LED-strip length modals) converts a drawn `{xPct,yPct}` path's percentage deltas into real feet using `area.photoWidthFt` for the x-axis and `photoWidthFt/area.photoAspect` (the photo's real height in feet) for the y-axis — safe because `xPct`/`yPct` are always percentages of the photo's own rendered box, which by construction always matches its true `photoAspect` regardless of container size. Returns `null` (not `0`) when the area has no known scale, so `openWireDistanceModal`/`openStripLengthModal` can tell "unmeasurable" apart from "really zero" and fall back to the pre-existing empty/manual-entry field exactly as before. When a scale IS known, the field is pre-filled with the estimate (rounded to match each field's own `step`) and a small hint note appears ("Estimated from the satellite view's scale — adjust if needed") — the value is a starting point, not locked, since a real wire route can snake around obstacles the straight drawn line doesn't capture. Editing an *existing* wire run/strip (`openWireEdit`/`openStripEdit`) never re-estimates or overwrites a value already saved, even if the area has a scale — only a freshly-drawn path gets the estimate.

**CAD plans — the "Plans" tab (replaced the old wiring-plan export)**: a separate area for Kichler/LightPro-style lighting site plans, requested as a full overhaul of the old per-photo "Export wiring plan" flow (which is **gone**: the photo editor's Place-transformer / Set-scale / Export-wiring-plan chips, the transformer objects/modal, `state.placingTransformer`/`settingScale`, the wire-run modal's transformer/fixture-assignment fields, and `buildWiringPlanPdfBlob`/`estimateMaxRunFt` were all removed; freehand wire runs, satellite view, `photoWidthFt` and `pathLengthFt` stay because they feed wire-length auto-estimates; `areas.transformers` and legacy `wireRun.transformerId/fixtureUids` are left in the data untouched so nothing is lost). Plans **belong to a project** (`cad_plans.project_id`), are created from the Plans tab (`+ New plan`: pick project, name, upload a site image — always user-supplied, no auto-generated plans), and open in a fullscreen editor (`#planEditorScreen`).
- **Data**: `cad_plans` (`id uuid, company_id, project_id, name, background` [Storage URL under `{companyId}/plans/`], `bg_width/bg_height`, `px_per_ft` [two-click scale calibration], `scene` jsonb `{fixtures:[{id,fixtureId,x,y,rot}], transformers:[{id,modelId,name,x,y,tapV}], runs:[{id,transformerId,fixtureIds[],gaugeAwg|null,zone,calloutOff}]}`, `meta` jsonb `{notes,sheetNo}`) and `cad_settings` (one row per company: `{symbolScale,minLampV,gauges,models,fixtureSymbol,customSymbols}`). Scene coordinates are the background image's own pixels. **Needs a one-time SQL migration** (bottom of `supabase/schema.sql`, RLS via `current_company_id()`); until it's run `cadLoad` sets `cadSchemaMissing` and the Plans tab shows a notice with `+ New plan` disabled — nothing else in the app is affected. `cadLoad` runs at the end of `loadCompanyDataFromDb`; the open plan autosaves (debounced 1.5s via `cadMarkDirty`, plus `flushCurrentEdits`'s tick and on close).
- **One pipeline, two backends (the hard requirement)**: `cadCalc(plan)` (pure) → display lists of primitives (`line/poly/circle/rect/text/image`) built by `cadSiteItems` (the drawing, in plan pixels; sizes scale with `u = bgW/520` so it looks the same at any zoom/page), `cadLeftBlocks` (schedule/legend/tables, in sheet units) and `cadBuildSheet` (pages: border, faded background, mapped site items, scale bar, left column packed into pages, title block) → drawn by **only two functions that touch a surface**: `cadDrawItemsCanvas` (editor, live schedule panel, sheet preview) and `cadDrawItemsPdf` (jsPDF vector; tabloid = the same letter-space sheet scaled by `k`). The interactive editor is `cadSiteItems({interactive:true,...})` — selection halos, in-progress run, snap ring, marquee, scale points are just extra primitives — not a second renderer. Don't add drawing logic to either backend; add a primitive/builder instead.
- **Symbols follow the attached Kichler/LightPro sheet** (color-coded circles with a centre dot, diamond bracket/connector marks, orange transformer box with a load callout, red/blue dashed wire by gauge) and are **data, not code** so they can change later: `CAD_BUILTIN_SYMBOLS` (shapes in a ±10 box: circle/rect/line/poly), per-type default pools (`CAD_TYPE_POOL`, so two different accents get different marks), a per-fixture override map (`cadSettings.fixtureSymbol`, edited in Settings or the inspector) and user JSON `customSymbols` (validated by `cadCleanSymbol` — numbers clamped, colors whitelisted — before reaching canvas/PDF/innerHTML SVG). Legend rows are per placed product (`symbol + name + count × VA`), plus accessories, transformers, and wire by gauge with total drawn footage.
- **Calculations** (`cadCalc`): run load = Σ fixture watts; run length = polyline transformer → fixtures **in click order** ÷ `px_per_ft`; max run length = `(tapV − minLampV) ÷ (2 × R/1000 × (VA ÷ tapV))` (generalises the old 15V-tap/4.5V calibration against the Kichler table; `minLampV` 10.5 → 4.5V drop at the 15V tap, editable); **auto gauge = smallest wire whose max ≥ drawn length** (default gauges 12/14/16/18 — 10 and 8 were removed on request; `gaugesVersion:2` in `cad_settings` so a company's older saved 12/10/8 list is ignored rather than resurrecting them), manual override per run flags `over` when drawn > max; transformer load/remaining with **80% warn / >100% over** (the app's existing 80% convention); zones are just a per-run number; warnings list (no scale, unassigned run, over-length, overload, fixture on multiple runs, fixture not on any run). The voltage-drop reference table on the sheet is generated from the same gauges/taps.
- **Editor** (`cadOnDown/Move/Up`, tools V select · H pan · F fixture · T transformer · R wire run · S scale): click-order run drawing with snap-to-node (click a transformer, then fixtures; double-click/Enter/Esc commits; right-click cancels), `+ Add fixtures` to extend an existing run, reorder/remove fixtures in the inspector, reassign transformer, gauge override, draggable run callouts (`calloutOff`), shift-click + marquee multi-select and group drag, Delete, snapshot undo/redo (`cadPush` before every mutation), wheel zoom / Space-drag pan. The right panel is the inspector plus a **live schedule** canvas drawing the same `cadLeftBlocks` the PDF uses. "Sheet & export" shows a multi-page preview and exports Letter or Tabloid PDF; the background is re-encoded as a downscaled JPEG for the PDF (a raw PNG made an 8MB file).
- **Second round (user requests)**: (1) **Palette**: separate **Fixture** and **Accessory** tools/buttons (accessories are catalog items with `cat:'accessory'`, minus transformers/cable), fixtures get a **type filter** (`FIXTURE_TYPE_ORDER`) + search; accessories have no `type` in the catalog so they only get search. (2) **Wire paths**: a run is no longer straight fixture-to-fixture — `run.bends["fromId|toId"] = [{x,y}…]` are waypoints between two consecutive nodes (keyed by node pair so they survive reordering); click empty space while drawing to add a bend, in the select tool double-click a wire to add one, drag the square handles to shape it, double-click a handle to remove it, Backspace steps back while drawing, and `run.smooth` renders a Catmull-Rom curve through every node/bend (`cadSmoothPath`). `cadRunPoints` returns the *real* path, and the drawn length, callout midpoint, hit-testing and rendering all read from it, so bends/curves change the calculated length and gauge. Runs are now drawn as one dashed **`poly`** (poly primitives gained `dash` in both backends) instead of per-segment lines, so the dash pattern is continuous around bends. (3) **Run info box** (callout): drag the body to move it (it already was), drag the amber corner square (`callout-resize`) to scale it (`run.calloutScale`, 0.5–4, stored alongside `calloutOff`; the box scales about its centre, and `calloutOff` is materialised on the first resize so it doesn't jump). (4) **Text notes with arrows**: `scene.notes = [{id,text,x,y,size,bg,arrow}]`, "Text note" tool; the note scales as a whole (`size`, corner-drag or slider), `bg:false` drops the box, and `arrow` is `{fixtureId,x,y}` (attached — the tip follows the fixture live) or `{x,y}` (free). Arrow geometry lives in `cadNoteArrowGeom`, and the amber drag handle sits **mid-shaft for an attached arrow and at the tip only for a free one** — at the tip it sat directly on the fixture and made that fixture undraggable while the note was selected (caught in testing). Dropping a note on a fixture's arrow re-attaches on release (`cadNearestNode`). Notes are ordinary primitives in `cadSiteItems`, so they print/export automatically. **Gotcha fixed in testing**: a scene created before `notes` existed (or by `cadCreatePlan`) has no `notes` array — `cadNormalizeScene` runs on open and creation now includes `notes:[]`.
- **A run can be extended from ANY fixture already on it, not just its tail** (`cadExtendFrom`, mirrored as `cadDraft.from` while drawing a brand-new run) — for tapping a new branch off a fixture partway along an already-wired run, not only continuing from wherever it currently ends. `run.fixtureIds` is still a single ordered array (no tree/graph rewrite), so this works by **re-anchoring**: clicking a node already on the run (any fixture, or the run's own transformer) while extending/drafting sets it as the current attach point instead of changing anything yet; the *next* new fixture clicked is then `splice`d into the array right after that anchor's index (`indexOf(anchor)+1` — naturally `0` when the anchor is the transformer, since it's never itself in `fixtureIds`) rather than always `push`ed to the end, and the anchor then advances to that new fixture so a run of clicks without re-anchoring still chains exactly like before. The wire path — and therefore the drawn length/gauge calc — routes through the new fixture(s) before whatever used to follow the anchor point, which is the one real tradeoff of staying with a flat array instead of a true branching-tree model: it re-routes that downstream stretch through the new fixtures rather than leaving it as an untouched parallel spur. Clicking a *different* run's transformer while extending/drafting this one is explicitly rejected (both the click handler and the hover snap-ring check `n.id !== (this run's) transformerId`) so it can't be mistaken for a valid attach point. **Backspace-to-step-back while drafting a brand-new run had to change too**: it used to always `.pop()` the array's last element, which is wrong once insertion isn't always at the end — it now undoes whichever fixture `cadDraft.from` (the current anchor) points to, wherever that actually sits in the array, and reattaches the anchor to whatever now precedes it. Verified live against the real database: drew transformer→A→B→C, re-anchored to the transformer itself and added D (landed at index 0, i.e. right after the transformer — `[D,A,B,C]`), then re-anchored to B (a true middle fixture) and added E, which landed correctly between B and C (`[D,A,B,E,C]`, visibly branching off B on the canvas), and confirmed Ctrl+Z correctly unwound the last insertion.
- **Transformers are a library category, not just a CAD setting**: the Fixtures screen has a fourth tab/category **Transformers** (`cat:'transformer'`, alongside fixture/accessory/strip; stored in `custom_fixtures.data` so no schema change) with the fields Name (= the **part number** printed on plans), Description, **Capacity (W)** (`capacityW`), **Voltage taps** (`taps`, comma list) and cost/price/distributor; the form hides Wattage for it, the "Add" button now defaults a new item's category to the tab you're on, and the Excel import recognises `Category = Transformer` (columns M/N: Transformer Capacity (W) and Voltage Taps — see the Excel import section). In CAD plans the Transformer tool lists these library parts (`cadLibraryTransformers`, also picks up legacy *accessories* named "…Transformer", parsing "300W" from the name); a placed transformer stores `catalogId` (+ `modelName`/`capacityW` snapshots as a fallback if the part is later deleted), and `cadModelFor` resolves capacity/taps **live from the library item**, so editing the part updates every plan; the inspector's "Part / model" select lists library parts first, then the generic Settings models. The Settings "generic transformer models" are now only a fallback for a library with no transformers. **Transformers placed on a project's plans are quoted**: `computeProjectLineItems` adds `cadPlanTransformerCounts(pr)` (library-linked transformers only — generic Settings models have no price) to the project quote, cost/margin, install time, the project-list total and the PO groups (by the part's distributor); the customer "show type instead of name" toggle shows a transformer's description like an accessory's. Deliberately **only transformers**, not plan fixtures/accessories: a plan's fixtures are normally also placed on the project's photo designs, so adding them would double-count, whereas transformers exist only on plans (the photo editor has none). Line order is now fixture → accessory → transformer → strip (`lineCatRank`). The per-area totals (`computeAreaLineItems`) are unchanged since a plan belongs to the project, not an area. **Transformers are also in the photo design editor's fixture library** (a **Transformers** tab in the Fixture library sheet, `data-cat="transformer"`): they use the same +/− stepper and `area.accessoryQty` storage as accessories (no schema change; `computeAreaLineItems` needed no change), with a capacity/taps spec line, and the empty tab points to Fixtures → Transformers. **Double-count rule:** since the same physical transformer is normally both chosen in a design and placed on the CAD plan, `computeProjectLineItems` now takes, per transformer id, `max(quantity added in designs, quantity placed on plans)` instead of the sum (`cadPlanTransformerCounts(...).forEach(... Math.max ...)`). This also covers the older case of a legacy *accessory* named "…Transformer" used in both places. Trade-off: two genuinely different units, one only in a design and one only on a plan, are under-counted — bump the design quantity in that case. Verified live: added a library transformer, set qty 2 in a design, quote showed 2 × price, persisted in `areas.accessory_qty`; the plan-vs-design `max` was checked by reading only (no plan with a placed transformer was built in that test).
- **A placed transformer can carry an "existing load" figure** (`t.existingLoadW`, plain number on the scene transformer object, no schema change since `scene` is jsonb) — for tying a new plan into a transformer that already has other fixtures on it, not part of this plan (so there's nothing to draw for that load, just a number to account for). An "Existing load already on this transformer (W)" input sits in the transformer inspector, right under Voltage tap. **`cadCalc`'s transformer `load` (and everything derived from it — `remaining`, `pct`, `status`, the warnings list, the canvas VA badge, and the printed capacity table) is always the TRUE total** (`newLoad` — just this plan's own runs — plus `existing`), so overload/80%-warning checks are correct once an existing load is entered; `newLoad` and `existing` are also returned separately for anywhere that wants to show the breakdown. The inspector shows a small "`Xw` from this plan + `Yw` existing" note once `existing>0`. The printed/live schedule's per-transformer table keeps its "Total" row as just this plan's runs (matches the run rows listed right above it) and adds a separate "Existing load (not on this plan)" row beneath it when `existing>0`, with the CAPACITY/LOAD/REMAINING CAPACITY row below both of those using the combined total. Verified live against the real database: entered 150W existing on a 300W transformer (Load 150/Capacity 300/Remaining 150), crossed the 80% warning at 260W and the overload flag at 350W, confirmed `existingLoadW` persisted in `cad_plans.scene` and survived a reload.
- **Verified** with a throwaway copy of the app whose `cad_plans`/`cad_settings` calls were swapped for an in-memory stub (the tables can't be created from this environment): create-from-image, scale, place, runs, edit, marquee/group drag/undo/redo, over-length and overload flags, symbol reassignment, custom symbols, persistence across reload, and a real exported PDF rendered with PyMuPDF — plus a regression pass on the photo editor after the removals. **Not yet exercised against the real tables** (run the SQL first), and desktop-oriented (no touch/pinch handling in the plan canvas).

**"Night Shift" visual restyle (appearance only, no functional changes)**: the app's core dark-theme color tokens (`--ink`/`--ink-2`/`--surface`/`--surface-2`/`--line` in `:root`) were retinted from a fairly neutral charcoal toward a more saturated navy (`#10182C` family instead of `#14171C`), because the whole app already reads its colors through these tokens — this one change cascades consistently through nav, tabs, cards, and modals without touching individual component CSS. `--amber`/`--amber-soft`/`--fog`/`--good`/`--danger` are unchanged, and `html[data-theme="light"]`'s tokens are untouched (light theme wasn't part of this pass). `.btn`'s existing `border-radius:9px` already matched the rectangular-not-pill button language this direction called for, so no button-shape change was needed anywhere.
- Landing page got three concrete additions, not just a recolor: small dots near the hero and a "product preview" card (a fake quote-line list resting on the dark hero with a deep shadow, `.landing-preview`) — both use hardcoded hex values matching **`FIXTURE_TYPES`' real identity colors** (`index.html`'s `FIXTURE_TYPES` — amber/green/purple/teal), not invented decorative colors, so the marketing page's palette is literally drawn from the same color system already used for placement markers and the legend inside the actual product. The 4 feature cards (`.landing-feature`) dropped their old icon-badge treatment for a colored top border + colored heading (`--feature-accent`, set inline per card) using that same palette, cycling through 4 of the 7 real fixture-type colors.
- **Print/export surfaces (`.po-doc` and the print stylesheet's fixed hex colors) were deliberately left untouched** — those already use their own hardcoded, print-stable colors independent of the `:root` theme tokens (see the print-background note above for why), and this pass was scoped to the interactive app + landing page, not exported documents. If a fully consistent navy identity in printed/exported output is wanted later, that's a separate, deliberate follow-up, not an oversight here.
- This was explored first as throwaway mockups (an Artifact comparing several directions pulled from real published `DESIGN.md` design-token documents — Mastercard, Starbucks, Apple, Notion — via `github.com/VoltAgent/awesome-design-md`) before any code changed; "Night Shift" (adapted from Notion's own documented system) is the one that was picked to actually build.

**Brand mark: "The Beam"** — the small icon next to the "Duskline" wordmark (nav bar `.brand-dot`, `favicon.svg`, `favicon-32.png`/`apple-touch-icon.png`, `og-image.png`) is a miniaturized version of the app's own real beam shape — a soft-edged amber cone narrowing to a bright point, the exact visual language of `buildBeamHtml`'s placed-fixture beams, not a generic dot or lighting-bolt icon. Picked from a 3-option review (an Artifact comparing this against a literal "dusk" horizon-line-with-a-setting-light mark, and a refined version of the plain dot it replaced) — chosen specifically because the mark **is** what the product does, not just a decorative logo. `.brand-dot` went from a CSS-only `radial-gradient` background on a `<span>` to an inline `<svg>` (the triangular beam shape can't be expressed as a simple radial gradient the way the old dot could) — two separate inline copies exist (landing nav + interior app topbar), each with its own gradient `id` suffix (`beamL`/`beamT`) since both live in the DOM simultaneously and SVG `id`s must be unique per document, even though only one is ever visible at a time. If this mark is ever revisited, regenerate `favicon-32.png`/`apple-touch-icon.png`/`og-image.png` too (headless Chrome screenshot of a sized HTML wrapper around the same SVG, same technique already used for the originals) — they're static rasters, not references to the SVG, so editing `favicon.svg` alone won't update them.

**Design notes** (`areas.notes` text column, "Notes" chip in the area editor's `#canvasToolbar`, `#notesModal`): free-text internal notes per area, edited via `openNotesModal`/`saveNotesModal` (persists immediately through `saveAreaRow`), shown on the project page's area card (`.area-card-notes`, 3-line clamp, escaped) and deliberately hidden in `@media print` — they're internal, never on the customer quote. The chip reads "Notes •" and gets `.is-active` styling once notes exist (`updateNotesBtn`, called from `refreshEditorPhotoState`). **Needs a one-time SQL migration** (`alter table public.areas add column if not exists notes text;`, at the bottom of `supabase/schema.sql`) — not run from this environment. Two safeguards so the missing column can't hurt anyone: `areaToRow` only includes `notes` once an area has ever used it (`area.notes || area._hadNotes` — `_hadNotes` also covers "loaded non-empty, then cleared"), so areas that never touch notes keep saving on a DB without the column; and `saveAreaRow` retries without `notes` on a "'notes' column" PostgREST error (with a one-time alert), because a failed `.update()` there is otherwise a silent console-warn that drops every OTHER edit on the row (placements, wires) too. Verified live on a DB without the column: modal/button/card work, HTML in a note is escaped, the design still saves. **The note itself does not persist across reloads until the SQL is run.** The button lives in `#canvasToolbar`, which is only visible once the area has a photo.

**Renaming an area after creation**: the "New area" dialog (`#areaNameModal`) doubles as "Rename area" — `openAreaNameModal(areaId|null)` sets the title/button (`Rename area`/`Save` vs `New area`/`Create`) and `areaRenameId` tells the shared `#areaNameCreate` handler which path to take. Entry points: a **Rename** button on each project-page area card (`[data-rename]`, hidden in print via `.area-card-body .btn`) and clicking the area editor's title (`#editorAreaTitle`, shows a ✎ and is keyboard-focusable). A rename only touches `area.name` and saves immediately through `saveAreaRow`; a blank name keeps the old one; Enter in the input submits. Verified live (editor title, card button, Enter, blank, persisted in `areas.name`, create dialog still says "New area" afterwards).

**Crop a CAD plan's background photo, after import** (`Crop photo` tool, `V/H/F/A/T/R/N/S/C` shortcut `C`, `cadCropRect`/`cadCropDrag`/`cadApplyCrop`): a plan's site image (uploaded or satellite-captured) can be trimmed at any time from inside the plan editor, not just at creation. Entering the tool starts a box covering the whole image; drag any of its 4 corner handles to resize it, drag inside it to move it, or drag anywhere outside it to draw a brand-new box — all via the same canvas the rest of the editor uses (`cadOnDown`/`cadOnMove` branches, rendered as a dashed box + dimmed exterior by `cadSiteItems`' interactive block, same primitive-list/two-backend architecture as everything else here — no new screen). The left panel shows the kept size in px (and feet, if the plan's scale is set) plus Reset/Cancel/Apply buttons; Enter applies, Escape cancels (falls through the existing tool-switch Escape handling).
- **Applying** re-draws just the kept rectangle from the already-loaded `cadImg` onto an offscreen canvas at 1:1 (no resampling) and re-uploads it to the *same* Storage path (`uploadToPhotosBucket` upserts + cache-busts), so the id/extension never change and nothing is orphaned. `cadPlan.bgW`/`bgH` are set to the new size; **`pxPerFt` is left untouched** since a pure crop doesn't resample — one image pixel still covers the same real-world distance it did before.
- **Every placed item's x/y is in background-image pixel space** (fixtures, transformers, notes, run bends — see the other CAD plans notes above), so the crop box's own top-left corner is subtracted from all of them, or everything would render shifted relative to the new, smaller image. `calloutOff` (a run's callout position) is a relative offset from a computed midpoint that itself re-derives from the (now-shifted) node positions each render, so it needs no adjustment; an arrow attached to a fixture reads that fixture's own already-shifted live position (`cadNoteTip`) rather than storing a coordinate of its own — only a *free* (unattached) note arrow's stored x/y needs shifting like everything else.
- **If a placed item would land outside the box being kept, a confirm dialog warns before proceeding** ("will end up off the plan") — cropping still allows it (the item is kept, just off-canvas) rather than silently deleting anything.
- **This is an explicit, immediately-saved action outside the undo stack**, the same category as creating the plan itself — a background-image change plus a scene-wide coordinate shift doesn't fit the scene-only undo snapshot (`cadApplySnapshot` only restores `scene`/`pxPerFt`, not `background`/`bgW`/`bgH`), so `cadPush()` is deliberately not called here; Ctrl+Z won't undo a crop.
- Verified against the real database: placed a transformer, cropped with the North-west/South-east corner handles (real OS-level drags — synthetic `PointerEvent` dispatch in test scripts round-tripped through `getBoundingClientRect()` inconsistently and is not to be trusted for this canvas's coordinate math), confirmed `bg_width`/`bg_height`/`background` (new cache-busted URL, same path) updated and the transformer's stored x/y shifted by exactly the crop box's own origin, and confirmed it survives a full reload. Also confirmed the "outside the kept area" confirm dialog fires when it should.

**Text boxes on the design photo** (`area.textBoxes` = `[{id,text,xPct,yPct}]`, `areas.text_boxes` jsonb column, "Add text" chip in `#canvasToolbar`, `#textBoxModal`): free-text callouts placed directly on the photo (distinct from the per-area "Design notes" above, which is a private text field). "Add text" opens the modal and drops the box near the middle (staggered so several don't stack) — deliberately *not* an "arm then click to place" tool like fixtures/transformers, since that would have needed threading a new mutually-exclusive mode through the ~6 places `state.placingTransformer`/`settingScale` are toggled; the user drags it into position instead (`makeDraggable`, click-without-drag reopens the modal with a Delete button). Rendered as `.text-box` in `renderOverlays()` (live, draggable), `buildStaticOverlaysHtml()` (project-page thumbnail and print) and `drawTextBoxOnCanvas()` (JPG export, wrapped to ~34% of the photo's width). **Sized in `cqw` (container-query width units)** so text scales with the photo in every place it's drawn (editor incl. zoom, thumbnail, print) — that required `container-type:inline-size` on `.canvas-photo` and `.area-card-photo` (NOT on `.overlays`: layout containment makes an element a stacking context, which would have cut `.beam-group`'s `plus-lighter` blend off from the photo underneath); the JPG path mirrors the ratio by hand (`cw*0.023`). Print/JPG output is controlled by a "Show text notes on exported photos" checkbox (`#toggleExportTextBoxes`, default on → `body.no-export-textboxes` hides them in `@media print`; the same checkbox gates `drawTextBoxOnCanvas`); the thumbnail on the project page always shows them. Print uses a light box (white/dark text) so it survives a browser dropping background graphics. Included in undo snapshots (`snapshotDesign`/`applySnapshot`) and cleared by "Clear design". **Needs a one-time SQL migration** (`alter table public.areas add column if not exists text_boxes jsonb not null default '[]';`, bottom of `supabase/schema.sql`) — not run from this environment. Same two safeguards as `notes`: `areaToRow` only sends `text_boxes` once used (`textBoxes.length || _hadTextBoxes`), and `saveAreaRow` retries without whichever of `notes`/`text_boxes` the database reports missing (one-time alert), so the rest of the design still saves. Verified live on a DB without the column: add/drag/edit/delete/undo, HTML in text is escaped, thumbnail and JPG export render it, and saves still succeed. **Resizing**: an invisible corner grab zone (`.text-box-resize` — no visible handle, the cursor just turns into a resize arrow over the corner; editor only — not in the static/print markup) at each box's bottom-right corner scales the whole box via a per-box `size` (default 1, clamped 0.4–6, stored on the box inside the same `text_boxes` jsonb, so no extra schema) applied as `--tb-size` in `font-size:calc(max(9px,2.3cqw) * var(--tb-size,1))`. Each pointermove sets `size *= pointerDistanceFromCenter / currentHalfDiagonal` — the box is anchored by its center, so its corner lands under the pointer immediately and can't oscillate. `max-width` is `15em` (not cqw) and the box is `width:max-content` so the wrap width scales *with* the text; without `width:max-content` an absolutely-positioned box's shrink-to-fit width is capped by the space to the right of its `left`, so a big box at the photo's midline silently wrapped at half the photo's width (caught in testing). `drawTextBoxOnCanvas` mirrors both (`fontPx * size`, wrap at `fontPx*15`). **Text boxes don't persist across reloads until the SQL is run.** **Editing gotcha for this repo's tooling: `index.html` is CRLF throughout** — a scripted multi-line find/replace has to normalize `\r\n` first (the Edit tool handles it; raw Python string matching on `\n` silently matches nothing).

### Editing conventions specific to this file

- New UI elements are added as plain HTML in the relevant screen's markup block (search the `<!-- ===== SECTION ===== -->` banner comments in the body, ~lines 388–814), then wired up with `addEventListener` calls inside `init()` (~line 2741, the largest function in the file).
- Fixture/accessory/strip categories are distinguished by `cat: 'fixture' | 'accessory' | 'strip'` throughout — filtering and rendering logic branches on this field repeatedly (sheet lists, quote line items, import parsing).
- Money values are always formatted through the `money()` helper (~line 873); don't hand-roll currency formatting.
- Adding a new field to a project/area/company/fixture now touches three places, not one: a column in `supabase/schema.sql` (run via SQL Editor), both directions of the relevant `rowTo*`/`*ToRow` mapper function, and a sensible default read (`x.newField || default`) at call sites — existing rows in the database won't have the new column backfilled, and there's no migration runner to do it for you.
- **Always wrap user-entered text in `escapeHtml()` before interpolating it into an `innerHTML` template string** — customer names, area names, and fixture name/desc are all typed by users and end up in `<div>...=`${...}`...</div>`-style templates throughout the render functions. A real stored-XSS gap here (customer/area/fixture name rendered unescaped in several places — the projects list, area cards, fixture sheet/library lists, both quote tables) was found and fixed in the same session that added the CDN integrity hashes below; don't reintroduce it in new render code. Values set via `.textContent` or an `<input>`'s `.value` don't need this — only raw `innerHTML` string-building does.
- **The CDN `<script>` tags in `<head>` (SheetJS, `@supabase/supabase-js`, `heic-to`, `jspdf`, `jspdf-autotable`) are pinned to an exact version with a `sha384` `integrity` hash and `crossorigin="anonymous"`.** If you ever bump any of their versions, you must recompute the hash for the new file (e.g. `curl -s <url> | openssl dgst -sha384 -binary | openssl base64 -A`) and update both the `src` URL and `integrity` together — an unpinned version range (like the old `@supabase/supabase-js@2`) can't have a stable SRI hash at all, since the served file changes over time.
- **HEIC/HEIF photos (iPhone's default photo format) don't decode in any browser but Safari/WebKit** — a raw HEIC file handed to `Image()`/`<img>` on Chrome, Firefox, or Edge (any OS) just never fires `onload`, a real user-reported "can't open certain photos" bug. `convertHeicIfNeeded(file)` (near `handleFile`) transcodes it to a real JPEG Blob client-side via **`heic-to`** (WASM, wraps an actively-maintained libheif build) before it ever reaches the existing FileReader/Image()/canvas pipeline, which needed no other changes — used by both the area-photo picker (`handleFile`, covers `fileInput` and the camera-capture input) and the company-logo picker (`#logoInput`'s change handler), the two places a user picks an arbitrary photo.
  - **This library replaced an earlier attempt using `heic2any`, which genuinely could not decode some real-world files.** A real user-reported HEIC (a normal 24MP iPhone photo, `ftyp` brand `heic`/`heix`/`MiHB`/`MiHA`/`MiHE`/`MiPr` — Apple's multi-resolution/progressive HEIC extension) failed with `heic2any`'s bundled (older, less-maintained) libheif WASM build every time — `ERR_LIBHEIF format not supported`, confirmed by actually running the exact failing file through the real pipeline (served locally, fetched into a real browser, converted) rather than guessing from unrelated smaller sample files, which had all worked fine and masked the real issue for a while. `heic-to`'s more current libheif build decodes the same file correctly. If HEIC support ever breaks again, this is the first thing to suspect — try the actual failing file directly against whichever library is current, don't assume small/simple test HEICs are representative of real iPhone output.
  - **`HeicTo.isHeic(file)` sniffs the real magic bytes rather than trusting `file.type`/the filename extension** (both unreliable — a file can be named/labeled `.heic` without actually being HEIC anymore, e.g. iCloud/Photos sometimes transparently converts a Live Photo to JPEG during transfer to a non-Apple device while the name stays `.heic`) — `convertHeicIfNeeded` checks this first and returns the file as-is with zero conversion attempted when it's not really HEIC, which is cleaner than the earlier approach of attempting a conversion and pattern-matching a specific rejection message to detect this case.
